-- THIS FILE WAS AUTOMATICALLY GENERATED BY AENEAS
-- [maledictus_call_binding_extraction]: function definitions
import Aeneas
import BindCallFull.Code.Types
import BindCallFull.Code.FunsExternal
open Aeneas Aeneas.Std Result ControlFlow Error
set_option linter.dupNamespace false
set_option linter.hashCommand false
set_option linter.unusedVariables false

/- You can set the `maxHeartbeats` value with the `-max-heartbeats` CLI option -/
set_option maxHeartbeats 1000000

/- You can set the `maxRecDepth` value with the `-max-recdepth` CLI option -/
set_option maxRecDepth 2048

/- You can remove the following line by using the CLI option `-all-computable`: -/
noncomputable section

namespace BindCallFull

/-- Trait implementation: [alloc::string::{impl core::cmp::PartialEq<alloc::string::String> for alloc::string::String}]
    Source: '/rustc/library/alloc/src/string.rs', lines 350:9-350:18
    Name pattern: [core::cmp::PartialEq<alloc::string::String, alloc::string::String>] -/
@[reducible, rust_trait_impl
  "core::cmp::PartialEq<alloc::string::String, alloc::string::String>"]
def alloc.string.String.Insts.CoreCmpPartialEqString : core.cmp.PartialEq
  String String := {
  eq := alloc.string.String.Insts.CoreCmpPartialEqString.eq
}

/-- [maledictus_call_binding_extraction::{impl core::clone::Clone for maledictus_call_binding_extraction::TypedValue<Type, Value>}::clone]:
    Source: 'src/lib.rs', lines 12:9-12:14
    Visibility: public -/
def TypedValue.Insts.CoreCloneClone.clone
  {«Type» : Type} {Value : Type} (corecloneCloneInst : core.clone.Clone
  «Type») (corecloneCloneInst1 : core.clone.Clone Value)
  (self : TypedValue «Type» Value) :
  Result (TypedValue «Type» Value)
  := do
  let t ← corecloneCloneInst.clone self.type_tag
  let t1 ← corecloneCloneInst1.clone self.value
  ok { type_tag := t, value := t1 }

/-- Trait implementation: [maledictus_call_binding_extraction::{impl core::clone::Clone for maledictus_call_binding_extraction::TypedValue<Type, Value>}]
    Source: 'src/lib.rs', lines 12:9-12:14 -/
@[reducible]
def TypedValue.Insts.CoreCloneClone {«Type» : Type} {Value : Type}
  (corecloneCloneInst : core.clone.Clone «Type») (corecloneCloneInst1 :
  core.clone.Clone Value) : core.clone.Clone (TypedValue «Type» Value) := {
  clone := TypedValue.Insts.CoreCloneClone.clone corecloneCloneInst
    corecloneCloneInst1
}

/-- [maledictus_call_binding_extraction::{impl core::clone::Clone for maledictus_call_binding_extraction::FormalParameter<Type, Value>}::clone]:
    Source: 'src/lib.rs', lines 25:9-25:14
    Visibility: public -/
def FormalParameter.Insts.CoreCloneClone.clone
  {«Type» : Type} {Value : Type} (corecloneCloneInst : core.clone.Clone
  «Type») (corecloneCloneInst1 : core.clone.Clone Value)
  (self : FormalParameter «Type» Value) :
  Result (FormalParameter «Type» Value)
  := do
  let s ← alloc.string.String.Insts.CoreCloneClone.clone self.name
  let t ← corecloneCloneInst.clone self.expected_type
  let o ←
    core.option.Option.Insts.CoreCloneClone.clone
      (TypedValue.Insts.CoreCloneClone corecloneCloneInst corecloneCloneInst1)
      self.default_value
  ok { «name» := s, expected_type := t, default_value := o }

/-- [maledictus_call_binding_extraction::{impl core::default::Default for maledictus_call_binding_extraction::CallSignature<Type, Value>}::default]:
    Source: 'src/lib.rs', lines 67:4-75:5
    Visibility: public -/
def CallSignature.Insts.CoreDefaultDefault.default
  («Type» : Type) (Value : Type) :
  Result (CallSignature «Type» Value)
  := do
  ok
    {
      positional_only := (alloc.vec.Vec.new (FormalParameter «Type» Value)),
      positional := (alloc.vec.Vec.new (FormalParameter «Type» Value)),
      keyword_only := (alloc.vec.Vec.new (FormalParameter «Type» Value)),
      var_args := none,
      keyword_args := none
    }

/-- [maledictus_call_binding_extraction::{impl core::clone::Clone for maledictus_call_binding_extraction::PositionalOrigin}::clone]:
    Source: 'src/lib.rs', lines 107:9-107:14
    Visibility: public -/
def PositionalOrigin.Insts.CoreCloneClone.clone
  (self : PositionalOrigin) : Result PositionalOrigin := do
  ok self

/-- [maledictus_call_binding_extraction::{impl core::clone::Clone for maledictus_call_binding_extraction::EvaluationPosition}::clone]:
    Source: 'src/lib.rs', lines 115:9-115:14
    Visibility: public -/
def EvaluationPosition.Insts.CoreCloneClone.clone
  (self : EvaluationPosition) : Result EvaluationPosition := do
  ok self

/-- [maledictus_call_binding_extraction::{impl core::clone::Clone for maledictus_call_binding_extraction::PositionalActual<Type, Value>}::clone]:
    Source: 'src/lib.rs', lines 141:9-141:14
    Visibility: public -/
def PositionalActual.Insts.CoreCloneClone.clone
  {«Type» : Type} {Value : Type} (corecloneCloneInst : core.clone.Clone
  «Type») (corecloneCloneInst1 : core.clone.Clone Value)
  (self : PositionalActual «Type» Value) :
  Result (PositionalActual «Type» Value)
  := do
  let tv ←
    TypedValue.Insts.CoreCloneClone.clone corecloneCloneInst
      corecloneCloneInst1 self.value
  let po ← PositionalOrigin.Insts.CoreCloneClone.clone self.origin
  let ep ←
    EvaluationPosition.Insts.CoreCloneClone.clone self.evaluation_position
  ok { value := tv, origin := po, evaluation_position := ep }

/-- [maledictus_call_binding_extraction::{impl core::clone::Clone for maledictus_call_binding_extraction::NamedActual<Type, Value>}::clone]:
    Source: 'src/lib.rs', lines 148:9-148:14
    Visibility: public -/
def NamedActual.Insts.CoreCloneClone.clone
  {«Type» : Type} {Value : Type} (corecloneCloneInst : core.clone.Clone
  «Type») (corecloneCloneInst1 : core.clone.Clone Value)
  (self : NamedActual «Type» Value) :
  Result (NamedActual «Type» Value)
  := do
  let s ← alloc.string.String.Insts.CoreCloneClone.clone self.name
  let tv ←
    TypedValue.Insts.CoreCloneClone.clone corecloneCloneInst
      corecloneCloneInst1 self.value
  let ep ←
    EvaluationPosition.Insts.CoreCloneClone.clone self.evaluation_position
  ok { «name» := s, value := tv, evaluation_position := ep }

/-- [maledictus_call_binding_extraction::{impl maledictus_call_binding_extraction::BindingAllocator for maledictus_call_binding_extraction::SystemBindingAllocator}::allocate]:
    Source: 'src/lib.rs', lines 288:4-294:5 -/
def
  SystemBindingAllocator.Insts.Maledictus_call_binding_extractionBindingAllocator.allocate
  (T : Type) (self : SystemBindingAllocator) (_site : AllocationSite)
  (requested : Std.Usize) :
  Result ((core.result.Result (alloc.vec.Vec T) Unit) ×
    SystemBindingAllocator)
  := do
  let (r, values) ←
    alloc.vec.Vec.try_reserve_exact Global (alloc.vec.Vec.new T) requested
  match r with
  | core.result.Result.Ok _ => ok (core.result.Result.Ok values, self)
  | core.result.Result.Err _ => ok (core.result.Result.Err (), self)

/-- Trait implementation: [maledictus_call_binding_extraction::{impl maledictus_call_binding_extraction::BindingAllocator for maledictus_call_binding_extraction::SystemBindingAllocator}]
    Source: 'src/lib.rs', lines 287:0-295:1 -/
@[reducible]
def
  SystemBindingAllocator.Insts.Maledictus_call_binding_extractionBindingAllocator
  : BindingAllocator SystemBindingAllocator := {
  allocate := fun {T : Type} =>
    SystemBindingAllocator.Insts.Maledictus_call_binding_extractionBindingAllocator.allocate
    T
}

/-- [maledictus_call_binding_extraction::allocate_buffer]:
    Source: 'src/lib.rs', lines 297:0-309:1 -/
def allocate_buffer
  («Type» : Type) (T : Type) {Allocator : Type} (BindingAllocatorInst :
  BindingAllocator Allocator) (allocator : Allocator) (site : AllocationSite)
  (requested : Std.Usize) :
  Result ((core.result.Result (alloc.vec.Vec T) (BindingError «Type»)) ×
    Allocator)
  := do
  let (r, allocator1) ←
    BindingAllocatorInst.allocate (T := T) allocator site requested
  match r with
  | core.result.Result.Ok values =>
    ok (core.result.Result.Ok values, allocator1)
  | core.result.Result.Err _ =>
    ok (core.result.Result.Err (BindingError.AllocationFailed site requested),
      allocator1)

/-- [maledictus_call_binding_extraction::{impl maledictus_call_binding_extraction::TypeCompatibility<Type> for maledictus_call_binding_extraction::ExactTypeCompatibility}::accepts]:
    Source: 'src/lib.rs', lines 318:4-320:5 -/
def
  ExactTypeCompatibility.Insts.Maledictus_call_binding_extractionTypeCompatibility.accepts
  {«Type» : Type} (corecmpEqInst : core.cmp.Eq «Type»)
  (self : ExactTypeCompatibility) (expected : «Type») (actual : «Type») :
  Result Bool
  := do
  core.cmp.impls.PartialEqShared.eq corecmpEqInst.partialEqInst expected actual

/-- Trait implementation: [maledictus_call_binding_extraction::{impl maledictus_call_binding_extraction::TypeCompatibility<Type> for maledictus_call_binding_extraction::ExactTypeCompatibility}]
    Source: 'src/lib.rs', lines 317:0-321:1 -/
@[reducible]
def
  ExactTypeCompatibility.Insts.Maledictus_call_binding_extractionTypeCompatibility
  {«Type» : Type} (corecmpEqInst : core.cmp.Eq «Type») : TypeCompatibility
  ExactTypeCompatibility «Type» := {
  accepts :=
    ExactTypeCompatibility.Insts.Maledictus_call_binding_extractionTypeCompatibility.accepts
    corecmpEqInst
}

/-- [maledictus_call_binding_extraction::compatibility_mismatch]:
    Source: 'src/lib.rs', lines 326:0-339:1 -/
def compatibility_mismatch
  {«Type» : Type} {Compatibility : Type} (TypeCompatibilityInst :
  TypeCompatibility Compatibility «Type») (compatibility : Compatibility)
  (expected : «Type») (actual : «Type») :
  Result Bool
  := do
  let b ← TypeCompatibilityInst.accepts compatibility expected actual
  if b
  then ok false
  else ok true

/-- [maledictus_call_binding_extraction::{impl maledictus_call_binding_extraction::TypeCompatibility<Type> for maledictus_call_binding_extraction::CallbackTypeCompatibility<Compatible>}::accepts]:
    Source: 'src/lib.rs', lines 347:4-349:5 -/
def
  CallbackTypeCompatibility.Insts.Maledictus_call_binding_extractionTypeCompatibility.accepts
  {«Type» : Type} {Compatible : Type}
  (coreopsfunctionFnCompatiblePairShared0TypeShared1TypeBoolInst :
  core.ops.function.Fn Compatible («Type» × «Type») Bool)
  (self : CallbackTypeCompatibility Compatible) (expected : «Type»)
  (actual : «Type») :
  Result Bool
  := do
  coreopsfunctionFnCompatiblePairShared0TypeShared1TypeBoolInst.call self
    (expected, actual)

/-- Trait implementation: [maledictus_call_binding_extraction::{impl maledictus_call_binding_extraction::TypeCompatibility<Type> for maledictus_call_binding_extraction::CallbackTypeCompatibility<Compatible>}]
    Source: 'src/lib.rs', lines 343:0-350:1 -/
@[reducible]
def
  CallbackTypeCompatibility.Insts.Maledictus_call_binding_extractionTypeCompatibility
  {«Type» : Type} {Compatible : Type}
  (coreopsfunctionFnCompatiblePairSharedTypeSharedTypeBoolInst :
  core.ops.function.Fn Compatible («Type» × «Type») Bool) :
  TypeCompatibility (CallbackTypeCompatibility Compatible) «Type» := {
  accepts :=
    CallbackTypeCompatibility.Insts.Maledictus_call_binding_extractionTypeCompatibility.accepts
    coreopsfunctionFnCompatiblePairSharedTypeSharedTypeBoolInst
}

/-- [maledictus_call_binding_extraction::string_list_contains]: loop body 0:
    Source: 'src/lib.rs', lines 1:0-1082:5 -/
@[rust_loop_body]
def string_list_contains_loop.body
  (names : Slice String) (candidate : String) (index : Std.Usize)
  (found : Bool) :
  Result (ControlFlow (Std.Usize × Bool) Bool)
  := do
  let i := Slice.len names
  if index < i
  then
    if found
    then ok (done true)
    else
      let s ← Slice.index_usize names index
      let found1 ←
        alloc.string.String.Insts.CoreCmpPartialEqString.eq s candidate
      let index1 ← index + 1#usize
      ok (cont (index1, found1))
  else ok (done found)

/-- [maledictus_call_binding_extraction::string_list_contains]: loop 0:
    Source: 'src/lib.rs', lines 1:0-1082:5 -/
@[rust_loop]
def string_list_contains_loop
  (names : Slice String) (candidate : String) (index : Std.Usize)
  (found : Bool) :
  Result Bool
  := do
  loop
    (fun (index1, found1) => string_list_contains_loop.body names candidate
      index1 found1)
    (index, found)

/-- [maledictus_call_binding_extraction::string_list_contains]:
    Source: 'src/lib.rs', lines 1076:0-1084:1 -/
@[reducible]
def string_list_contains
  (names : Slice String) (candidate : String) : Result Bool := do
  string_list_contains_loop names candidate 0#usize false

/-- [maledictus_call_binding_extraction::validate_formal_parameter]:
    Source: 'src/lib.rs', lines 1190:0-1232:1 -/
def validate_formal_parameter
  {«Type» : Type} {Value : Type} {Compatibility : Type} (corecloneCloneInst :
  core.clone.Clone «Type») (TypeCompatibilityInst : TypeCompatibility
  Compatibility «Type») (parameter : FormalParameter «Type» Value)
  (variadic : Bool) (names : alloc.vec.Vec String)
  (compatibility : Compatibility) :
  Result ((core.result.Result Unit (BindingError «Type»)) × (alloc.vec.Vec
    String))
  := do
  let b ← alloc.string.String.is_empty parameter.name
  if b
  then
    ok (core.result.Result.Err (BindingError.MalformedSignature
      MalformedSignature.EmptyParameterName), names)
  else
    let s := alloc.vec.Vec.deref names
    let b1 ← string_list_contains s parameter.name
    if b1
    then
      let s1 ← alloc.string.String.Insts.CoreCloneClone.clone parameter.name
      ok (core.result.Result.Err (BindingError.MalformedSignature
        (MalformedSignature.DuplicateParameter s1)), names)
    else
      let s1 ← alloc.string.String.Insts.CoreCloneClone.clone parameter.name
      let names1 ← alloc.vec.Vec.push names s1
      if variadic
      then
        let b2 := core.option.Option.is_some parameter.default_value
        if b2
        then
          ok (core.result.Result.Err (BindingError.MalformedSignature
            (MalformedSignature.VariadicDefault s1)), names1)
        else
          match parameter.default_value with
          | none => ok (core.result.Result.Ok (), names1)
          | some default =>
            let b3 ←
              TypeCompatibilityInst.accepts compatibility
                parameter.expected_type default.type_tag
            if b3
            then ok (core.result.Result.Ok (), names1)
            else
              let t ← corecloneCloneInst.clone parameter.expected_type
              let t1 ← corecloneCloneInst.clone default.type_tag
              ok (core.result.Result.Err (BindingError.MalformedSignature
                (MalformedSignature.DefaultTypeMismatch s1 t t1)), names1)
      else
        match parameter.default_value with
        | none => ok (core.result.Result.Ok (), names1)
        | some default =>
          let b2 ←
            TypeCompatibilityInst.accepts compatibility parameter.expected_type
              default.type_tag
          if b2
          then ok (core.result.Result.Ok (), names1)
          else
            let t ← corecloneCloneInst.clone parameter.expected_type
            let t1 ← corecloneCloneInst.clone default.type_tag
            ok (core.result.Result.Err (BindingError.MalformedSignature
              (MalformedSignature.DefaultTypeMismatch s1 t t1)), names1)

/-- [maledictus_call_binding_extraction::add_parameter_count]:
    Source: 'src/lib.rs', lines 622:0-635:1 -/
def add_parameter_count
  («Type» : Type) (accumulated : Std.Usize) (contribution : Std.Usize) :
  Result (core.result.Result Std.Usize (BindingError «Type»))
  := do
  let o ← lift (Usize.checked_add accumulated contribution)
  match o with
  | none =>
    ok (core.result.Result.Err (BindingError.MalformedSignature
      (MalformedSignature.ParameterCountOverflow accumulated contribution)))
  | some count => ok (core.result.Result.Ok count)

/-- [maledictus_call_binding_extraction::signature_counts]:
    Source: 'src/lib.rs', lines 637:0-653:1 -/
def signature_counts
  {«Type» : Type} {Value : Type} (signature : CallSignature «Type» Value) :
  Result (core.result.Result SignatureCounts (BindingError «Type»))
  := do
  let i := alloc.vec.Vec.len signature.positional_only
  let i1 := alloc.vec.Vec.len signature.positional
  let r ← add_parameter_count «Type» i i1
  let cf ← core.result.Result.Insts.CoreOpsTry.branch r
  match cf with
  | core.ops.control_flow.ControlFlow.Continue val =>
    let i2 := alloc.vec.Vec.len signature.keyword_only
    let r1 ← add_parameter_count «Type» val i2
    let cf1 ← core.result.Result.Insts.CoreOpsTry.branch r1
    match cf1 with
    | core.ops.control_flow.ControlFlow.Continue val1 =>
      let b := core.option.Option.is_some signature.var_args
      let i3 ← lift (core.convert.num.FromUsizeBool.from b)
      let r2 ← add_parameter_count «Type» val1 i3
      let cf2 ← core.result.Result.Insts.CoreOpsTry.branch r2
      match cf2 with
      | core.ops.control_flow.ControlFlow.Continue val2 =>
        let b1 := core.option.Option.is_some signature.keyword_args
        let i4 ← lift (core.convert.num.FromUsizeBool.from b1)
        let r3 ← add_parameter_count «Type» val2 i4
        let cf3 ← core.result.Result.Insts.CoreOpsTry.branch r3
        match cf3 with
        | core.ops.control_flow.ControlFlow.Continue val3 =>
          ok (core.result.Result.Ok
            { parameter_count := val3, positional_count := val })
        | core.ops.control_flow.ControlFlow.Break residual =>
          core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
            SignatureCounts (core.convert.FromSame (BindingError «Type»))
            residual
      | core.ops.control_flow.ControlFlow.Break residual =>
        core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
          SignatureCounts (core.convert.FromSame (BindingError «Type»))
          residual
    | core.ops.control_flow.ControlFlow.Break residual =>
      core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
        SignatureCounts (core.convert.FromSame (BindingError «Type»))
        residual
  | core.ops.control_flow.ControlFlow.Break residual =>
    core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
      SignatureCounts (core.convert.FromSame (BindingError «Type»)) residual

/-- [maledictus_call_binding_extraction::validate_signature]: loop body 0:
    Source: 'src/lib.rs', lines 1138:4-1149:5 -/
@[rust_loop_body]
def validate_signature_loop0.body
  {«Type» : Type} {Value : Type} {Compatibility : Type} (corecloneCloneInst :
  core.clone.Clone «Type») (TypeCompatibilityInst : TypeCompatibility
  Compatibility «Type»)
  (positional_only : alloc.vec.Vec (FormalParameter «Type» Value))
  (compatibility : Compatibility) (names : alloc.vec.Vec String)
  (index : Std.Usize) (validation_error : Option (BindingError «Type»)) :
  Result (ControlFlow ((alloc.vec.Vec String) × Std.Usize × (Option
    (BindingError «Type»))) ((alloc.vec.Vec String) × (Option (BindingError
    «Type»))))
  := do
  let i := alloc.vec.Vec.len positional_only
  if index < i
  then
    let b := core.option.Option.is_none validation_error
    let (names1, validation_error1) ←
      if b
      then
        do
        let fp ←
          alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
            (FormalParameter «Type» Value)) positional_only index
        let (r, names2) ←
          validate_formal_parameter corecloneCloneInst TypeCompatibilityInst fp
            false names compatibility
        let validation_error2 ← core.result.Result.err r
        ok (names2, validation_error2)
      else ok (names, validation_error)
    let index1 ← index + 1#usize
    ok (cont (names1, index1, validation_error1))
  else ok (done (names, validation_error))

/-- [maledictus_call_binding_extraction::validate_signature]: loop 0:
    Source: 'src/lib.rs', lines 1138:4-1149:5 -/
@[rust_loop]
def validate_signature_loop0
  {«Type» : Type} {Value : Type} {Compatibility : Type} (corecloneCloneInst :
  core.clone.Clone «Type») (TypeCompatibilityInst : TypeCompatibility
  Compatibility «Type»)
  (positional_only : alloc.vec.Vec (FormalParameter «Type» Value))
  (compatibility : Compatibility) (names : alloc.vec.Vec String)
  (index : Std.Usize) (validation_error : Option (BindingError «Type»)) :
  Result ((alloc.vec.Vec String) × (Option (BindingError «Type»)))
  := do
  loop
    (fun (names1, index1, validation_error1) => validate_signature_loop0.body
      corecloneCloneInst TypeCompatibilityInst positional_only compatibility
      names1 index1 validation_error1)
    (names, index, validation_error)

/-- [maledictus_call_binding_extraction::validate_signature]: loop body 1:
    Source: 'src/lib.rs', lines 1156:4-1163:5 -/
@[rust_loop_body]
def validate_signature_loop1.body
  {«Type» : Type} {Value : Type} {Compatibility : Type} (corecloneCloneInst :
  core.clone.Clone «Type») (TypeCompatibilityInst : TypeCompatibility
  Compatibility «Type»)
  (positional : alloc.vec.Vec (FormalParameter «Type» Value))
  (compatibility : Compatibility) (names : alloc.vec.Vec String)
  (index : Std.Usize) (validation_error : Option (BindingError «Type»)) :
  Result (ControlFlow ((alloc.vec.Vec String) × Std.Usize × (Option
    (BindingError «Type»))) ((alloc.vec.Vec String) × (Option (BindingError
    «Type»))))
  := do
  let i := alloc.vec.Vec.len positional
  if index < i
  then
    let b := core.option.Option.is_none validation_error
    let (names1, validation_error1) ←
      if b
      then
        do
        let fp ←
          alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
            (FormalParameter «Type» Value)) positional index
        let (r, names2) ←
          validate_formal_parameter corecloneCloneInst TypeCompatibilityInst fp
            false names compatibility
        let validation_error2 ← core.result.Result.err r
        ok (names2, validation_error2)
      else ok (names, validation_error)
    let index1 ← index + 1#usize
    ok (cont (names1, index1, validation_error1))
  else ok (done (names, validation_error))

/-- [maledictus_call_binding_extraction::validate_signature]: loop 1:
    Source: 'src/lib.rs', lines 1156:4-1163:5 -/
@[rust_loop]
def validate_signature_loop1
  {«Type» : Type} {Value : Type} {Compatibility : Type} (corecloneCloneInst :
  core.clone.Clone «Type») (TypeCompatibilityInst : TypeCompatibility
  Compatibility «Type»)
  (positional : alloc.vec.Vec (FormalParameter «Type» Value))
  (compatibility : Compatibility) (names : alloc.vec.Vec String)
  (index : Std.Usize) (validation_error : Option (BindingError «Type»)) :
  Result ((alloc.vec.Vec String) × (Option (BindingError «Type»)))
  := do
  loop
    (fun (names1, index1, validation_error1) => validate_signature_loop1.body
      corecloneCloneInst TypeCompatibilityInst positional compatibility names1
      index1 validation_error1)
    (names, index, validation_error)

/-- [maledictus_call_binding_extraction::validate_signature]: loop body 2:
    Source: 'src/lib.rs', lines 1170:4-1177:5 -/
@[rust_loop_body]
def validate_signature_loop2.body
  {«Type» : Type} {Value : Type} {Compatibility : Type} (corecloneCloneInst :
  core.clone.Clone «Type») (TypeCompatibilityInst : TypeCompatibility
  Compatibility «Type»)
  (keyword_only : alloc.vec.Vec (FormalParameter «Type» Value))
  (compatibility : Compatibility) (names : alloc.vec.Vec String)
  (index : Std.Usize) (validation_error : Option (BindingError «Type»)) :
  Result (ControlFlow ((alloc.vec.Vec String) × Std.Usize × (Option
    (BindingError «Type»))) ((alloc.vec.Vec String) × (Option (BindingError
    «Type»))))
  := do
  let i := alloc.vec.Vec.len keyword_only
  if index < i
  then
    let b := core.option.Option.is_none validation_error
    let (names1, validation_error1) ←
      if b
      then
        do
        let fp ←
          alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
            (FormalParameter «Type» Value)) keyword_only index
        let (r, names2) ←
          validate_formal_parameter corecloneCloneInst TypeCompatibilityInst fp
            false names compatibility
        let validation_error2 ← core.result.Result.err r
        ok (names2, validation_error2)
      else ok (names, validation_error)
    let index1 ← index + 1#usize
    ok (cont (names1, index1, validation_error1))
  else ok (done (names, validation_error))

/-- [maledictus_call_binding_extraction::validate_signature]: loop 2:
    Source: 'src/lib.rs', lines 1170:4-1177:5 -/
@[rust_loop]
def validate_signature_loop2
  {«Type» : Type} {Value : Type} {Compatibility : Type} (corecloneCloneInst :
  core.clone.Clone «Type») (TypeCompatibilityInst : TypeCompatibility
  Compatibility «Type»)
  (keyword_only : alloc.vec.Vec (FormalParameter «Type» Value))
  (compatibility : Compatibility) (names : alloc.vec.Vec String)
  (index : Std.Usize) (validation_error : Option (BindingError «Type»)) :
  Result ((alloc.vec.Vec String) × (Option (BindingError «Type»)))
  := do
  loop
    (fun (names1, index1, validation_error1) => validate_signature_loop2.body
      corecloneCloneInst TypeCompatibilityInst keyword_only compatibility
      names1 index1 validation_error1)
    (names, index, validation_error)

/-- [maledictus_call_binding_extraction::validate_signature]:
    Source: 'src/lib.rs', lines 1119:0-1188:1 -/
def validate_signature
  {«Type» : Type} {Value : Type} {Compatibility : Type} {Allocator : Type}
  (corecloneCloneInst : core.clone.Clone «Type») (TypeCompatibilityInst :
  TypeCompatibility Compatibility «Type») (BindingAllocatorInst :
  BindingAllocator Allocator) (signature : CallSignature «Type» Value)
  (compatibility : Compatibility) (allocator : Allocator) :
  Result ((core.result.Result SignatureCounts (BindingError «Type»)) ×
    Allocator)
  := do
  let r ← signature_counts signature
  let cf ← core.result.Result.Insts.CoreOpsTry.branch r
  match cf with
  | core.ops.control_flow.ControlFlow.Continue val =>
    let (r1, allocator1) ←
      allocate_buffer «Type» String BindingAllocatorInst allocator
        AllocationSite.SignatureNames val.parameter_count
    let cf1 ← core.result.Result.Insts.CoreOpsTry.branch r1
    match cf1 with
    | core.ops.control_flow.ControlFlow.Continue val1 =>
      let (names, validation_error) ←
        validate_signature_loop0 corecloneCloneInst TypeCompatibilityInst
          signature.positional_only compatibility val1 0#usize none
      match validation_error with
      | none =>
        let (names1, validation_error1) ←
          validate_signature_loop1 corecloneCloneInst TypeCompatibilityInst
            signature.positional compatibility names 0#usize none
        match validation_error1 with
        | none =>
          let (names2, validation_error2) ←
            validate_signature_loop2 corecloneCloneInst TypeCompatibilityInst
              signature.keyword_only compatibility names1 0#usize none
          match validation_error2 with
          | none =>
            match signature.var_args with
            | none =>
              match signature.keyword_args with
              | none => ok (core.result.Result.Ok val, allocator1)
              | some parameter =>
                let (r2, _) ←
                  validate_formal_parameter corecloneCloneInst
                    TypeCompatibilityInst parameter true names2 compatibility
                let cf2 ← core.result.Result.Insts.CoreOpsTry.branch r2
                match cf2 with
                | core.ops.control_flow.ControlFlow.Continue _ =>
                  ok (core.result.Result.Ok val, allocator1)
                | core.ops.control_flow.ControlFlow.Break residual =>
                  let r3 ←
                    core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
                      SignatureCounts (core.convert.FromSame (BindingError
                      «Type»)) residual
                  ok (r3, allocator1)
            | some parameter =>
              let (r2, names3) ←
                validate_formal_parameter corecloneCloneInst
                  TypeCompatibilityInst parameter true names2 compatibility
              let cf2 ← core.result.Result.Insts.CoreOpsTry.branch r2
              match cf2 with
              | core.ops.control_flow.ControlFlow.Continue _ =>
                match signature.keyword_args with
                | none => ok (core.result.Result.Ok val, allocator1)
                | some parameter1 =>
                  let (r3, _) ←
                    validate_formal_parameter corecloneCloneInst
                      TypeCompatibilityInst parameter1 true names3
                      compatibility
                  let cf3 ← core.result.Result.Insts.CoreOpsTry.branch r3
                  match cf3 with
                  | core.ops.control_flow.ControlFlow.Continue _ =>
                    ok (core.result.Result.Ok val, allocator1)
                  | core.ops.control_flow.ControlFlow.Break residual =>
                    let r4 ←
                      core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
                        SignatureCounts (core.convert.FromSame (BindingError
                        «Type»)) residual
                    ok (r4, allocator1)
              | core.ops.control_flow.ControlFlow.Break residual =>
                let r3 ←
                  core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
                    SignatureCounts (core.convert.FromSame (BindingError
                    «Type»)) residual
                ok (r3, allocator1)
          | some error => ok (core.result.Result.Err error, allocator1)
        | some error => ok (core.result.Result.Err error, allocator1)
      | some error => ok (core.result.Result.Err error, allocator1)
    | core.ops.control_flow.ControlFlow.Break residual =>
      let r2 ←
        core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
          SignatureCounts (core.convert.FromSame (BindingError «Type»))
          residual
      ok (r2, allocator1)
  | core.ops.control_flow.ControlFlow.Break residual =>
    let r1 ←
      core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
        SignatureCounts (core.convert.FromSame (BindingError «Type»))
        residual
    ok (r1, allocator)

/-- [maledictus_call_binding_extraction::preflight_actual_items]: loop body 0:
    Source: 'src/lib.rs', lines 581:4-608:5 -/
@[rust_loop_body]
def preflight_actual_items_loop.body
  {«Type» : Type} {Value : Type}
  (iter : core.iter.adapters.enumerate.Enumerate (core.slice.iter.Iter
  (ActualItem «Type» Value))) (expanded_count : Std.Usize)
  (preflight_error : Option (BindingError «Type»)) :
  Result (ControlFlow ((core.iter.adapters.enumerate.Enumerate
    (core.slice.iter.Iter (ActualItem «Type» Value))) × Std.Usize × (Option
    (BindingError «Type»))) (Std.Usize × (Option (BindingError «Type»))))
  := do
  let (o, iter1) ←
    core.iter.adapters.enumerate.IteratorEnumerate.next
      (core.iter.traits.iterator.IteratorSliceIter (ActualItem «Type» Value))
      iter
  match o with
  | none => ok (done (expanded_count, preflight_error))
  | some p =>
    let (item_index, item) := p
    let b := core.option.Option.is_none preflight_error
    if b
    then
      let (preflight_error1, contribution) ←
        match item with
        | ActualItem.Positional _ => ok (preflight_error, 1#usize)
        | ActualItem.Named _ _ => ok (preflight_error, 1#usize)
        | ActualItem.FixedStar values =>
          let contribution1 := alloc.vec.Vec.len values
          ok (preflight_error, contribution1)
        | ActualItem.DynamicStar =>
          ok (some (BindingError.DynamicStarUnsupported item_index), 0#usize)
        | ActualItem.KeywordMapping =>
          ok (some (BindingError.KeywordMappingUnsupported item_index),
            0#usize)
      let b1 := core.option.Option.is_none preflight_error1
      if b1
      then
        let o1 ← lift (Usize.checked_add expanded_count contribution)
        match o1 with
        | none =>
          ok (cont (iter1, expanded_count, some
            (BindingError.ExpandedArgumentCountOverflow item_index
            expanded_count contribution)))
        | some count => ok (cont (iter1, count, preflight_error1))
      else ok (cont (iter1, expanded_count, preflight_error1))
    else ok (cont (iter1, expanded_count, preflight_error))

/-- [maledictus_call_binding_extraction::preflight_actual_items]: loop 0:
    Source: 'src/lib.rs', lines 581:4-608:5 -/
@[rust_loop]
def preflight_actual_items_loop
  {«Type» : Type} {Value : Type}
  (iter : core.iter.adapters.enumerate.Enumerate (core.slice.iter.Iter
  (ActualItem «Type» Value))) (expanded_count : Std.Usize)
  (preflight_error : Option (BindingError «Type»)) :
  Result (Std.Usize × (Option (BindingError «Type»)))
  := do
  loop
    (fun (iter1, expanded_count1, preflight_error1) =>
      preflight_actual_items_loop.body iter1 expanded_count1 preflight_error1)
    (iter, expanded_count, preflight_error)

/-- [maledictus_call_binding_extraction::preflight_actual_items]:
    Source: 'src/lib.rs', lines 564:0-614:1 -/
def preflight_actual_items
  {«Type» : Type} {Value : Type} (items : Slice (ActualItem «Type» Value))
  (has_receiver : Bool) :
  Result (core.result.Result (Std.Usize × Std.Usize) (BindingError «Type»))
  := do
  let receiver_count ←
    lift (core.convert.num.FromUsizeBool.from has_receiver)
  let i := Slice.len items
  let o ← lift (Usize.checked_add i receiver_count)
  match o with
  | none =>
    let i1 := Slice.len items
    ok (core.result.Result.Err (BindingError.SourceExpressionCountOverflow i1
      receiver_count))
  | some count =>
    let i1 ← core.slice.Slice.iter items
    let iter ←
      core.iter.traits.iterator.Iterator.enumerate.trait_default
        (core.iter.traits.iterator.IteratorSliceIter (ActualItem «Type»
        Value)) i1
    let (expanded_count, preflight_error) ←
      preflight_actual_items_loop iter receiver_count none
    match preflight_error with
    | none => ok (core.result.Result.Ok (count, expanded_count))
    | some error => ok (core.result.Result.Err error)

/-- [maledictus_call_binding_extraction::expand_actual_items_with_allocator]: loop body 1:
    Source: 'src/lib.rs', lines 532:20-541:21 -/
@[rust_loop_body]
def expand_actual_items_with_allocator_loop0_loop0.body
  {«Type» : Type} {Value : Type} (corecloneCloneInst : core.clone.Clone
  «Type») (corecloneCloneInst1 : core.clone.Clone Value)
  (item_index : Std.Usize)
  (iter : core.iter.adapters.enumerate.Enumerate (core.slice.iter.Iter
  (TypedValue «Type» Value)))
  (positional : alloc.vec.Vec (PositionalActual «Type» Value)) :
  Result (ControlFlow ((core.iter.adapters.enumerate.Enumerate
    (core.slice.iter.Iter (TypedValue «Type» Value))) × (alloc.vec.Vec
    (PositionalActual «Type» Value))) (alloc.vec.Vec (PositionalActual
    «Type» Value)))
  := do
  let (o, iter1) ←
    core.iter.adapters.enumerate.IteratorEnumerate.next
      (core.iter.traits.iterator.IteratorSliceIter (TypedValue «Type» Value))
      iter
  match o with
  | none => ok (done positional)
  | some p =>
    let (expansion_index, value) := p
    let tv ←
      TypedValue.Insts.CoreCloneClone.clone corecloneCloneInst
        corecloneCloneInst1 value
    let positional1 ←
      alloc.vec.Vec.push positional
        ({
           value := tv,
           origin := PositionalOrigin.FixedStar,
           evaluation_position :=
             (EvaluationPosition.Actual item_index expansion_index)
         } : PositionalActual «Type» Value)
    ok (cont (iter1, positional1))

/-- [maledictus_call_binding_extraction::expand_actual_items_with_allocator]: loop 1:
    Source: 'src/lib.rs', lines 532:20-541:21 -/
@[rust_loop]
def expand_actual_items_with_allocator_loop0_loop0
  {«Type» : Type} {Value : Type} (corecloneCloneInst : core.clone.Clone
  «Type») (corecloneCloneInst1 : core.clone.Clone Value)
  (iter : core.iter.adapters.enumerate.Enumerate (core.slice.iter.Iter
  (TypedValue «Type» Value)))
  (positional : alloc.vec.Vec (PositionalActual «Type» Value))
  (item_index : Std.Usize) :
  Result (alloc.vec.Vec (PositionalActual «Type» Value))
  := do
  loop
    (fun (iter1, positional1) =>
      expand_actual_items_with_allocator_loop0_loop0.body corecloneCloneInst
      corecloneCloneInst1 item_index iter1 positional1)
    (iter, positional)

/-- [maledictus_call_binding_extraction::expand_actual_items_with_allocator]: loop body 0:
    Source: 'src/lib.rs', lines 499:4-551:5 -/
@[rust_loop_body]
def expand_actual_items_with_allocator_loop0.body
  {«Type» : Type} {Value : Type} (corecloneCloneInst : core.clone.Clone
  «Type») (corecloneCloneInst1 : core.clone.Clone Value)
  (iter : core.iter.adapters.enumerate.Enumerate (core.slice.iter.Iter
  (ActualItem «Type» Value)))
  (positional : alloc.vec.Vec (PositionalActual «Type» Value))
  (named : alloc.vec.Vec (NamedActual «Type» Value))
  (evaluation_order : alloc.vec.Vec EvaluationEvent)
  (expansion_error : Option (BindingError «Type»)) :
  Result (ControlFlow ((core.iter.adapters.enumerate.Enumerate
    (core.slice.iter.Iter (ActualItem «Type» Value))) × (alloc.vec.Vec
    (PositionalActual «Type» Value)) × (alloc.vec.Vec (NamedActual «Type»
    Value)) × (alloc.vec.Vec EvaluationEvent) × (Option (BindingError
    «Type»))) ((alloc.vec.Vec (PositionalActual «Type» Value)) ×
    (alloc.vec.Vec (NamedActual «Type» Value)) × (alloc.vec.Vec
    EvaluationEvent) × (Option (BindingError «Type»))))
  := do
  let (o, iter1) ←
    core.iter.adapters.enumerate.IteratorEnumerate.next
      (core.iter.traits.iterator.IteratorSliceIter (ActualItem «Type» Value))
      iter
  match o with
  | none => ok (done (positional, named, evaluation_order, expansion_error))
  | some p =>
    let (item_index, item) := p
    let b := core.option.Option.is_none expansion_error
    if b
    then
      match item with
      | ActualItem.Positional value =>
        let evaluation_order1 ←
          alloc.vec.Vec.push evaluation_order (EvaluationEvent.Positional
            item_index)
        let tv ←
          TypedValue.Insts.CoreCloneClone.clone corecloneCloneInst
            corecloneCloneInst1 value
        let positional1 ←
          alloc.vec.Vec.push positional
            ({
               value := tv,
               origin := PositionalOrigin.Explicit,
               evaluation_position :=
                 (EvaluationPosition.Actual item_index 0#usize)
             } : PositionalActual «Type» Value)
        ok (cont (iter1, positional1, named, evaluation_order1,
          expansion_error))
      | ActualItem.Named «name» value =>
        let s ← alloc.string.String.Insts.CoreCloneClone.clone «name»
        let evaluation_order1 ←
          alloc.vec.Vec.push evaluation_order (EvaluationEvent.Named item_index
            s)
        let tv ←
          TypedValue.Insts.CoreCloneClone.clone corecloneCloneInst
            corecloneCloneInst1 value
        let named1 ←
          alloc.vec.Vec.push named
            ({
               «name» := s,
               value := tv,
               evaluation_position :=
                 (EvaluationPosition.Actual item_index 0#usize)
             } : NamedActual «Type» Value)
        ok (cont (iter1, positional, named1, evaluation_order1,
          expansion_error))
      | ActualItem.FixedStar values =>
        let i := alloc.vec.Vec.len values
        let evaluation_order1 ←
          alloc.vec.Vec.push evaluation_order (EvaluationEvent.FixedStar
            item_index i)
        let s := alloc.vec.Vec.deref values
        let i1 ← core.slice.Slice.iter s
        let iter2 ←
          core.iter.traits.iterator.Iterator.enumerate.trait_default
            (core.iter.traits.iterator.IteratorSliceIter (TypedValue «Type»
            Value)) i1
        let positional1 ←
          expand_actual_items_with_allocator_loop0_loop0 corecloneCloneInst
            corecloneCloneInst1 iter2 positional item_index
        ok (cont (iter1, positional1, named, evaluation_order1,
          expansion_error))
      | ActualItem.DynamicStar =>
        ok (cont (iter1, positional, named, evaluation_order, some
          (BindingError.DynamicStarUnsupported item_index)))
      | ActualItem.KeywordMapping =>
        ok (cont (iter1, positional, named, evaluation_order, some
          (BindingError.KeywordMappingUnsupported item_index)))
    else
      ok (cont (iter1, positional, named, evaluation_order, expansion_error))

/-- [maledictus_call_binding_extraction::expand_actual_items_with_allocator]: loop 0:
    Source: 'src/lib.rs', lines 499:4-551:5 -/
@[rust_loop]
def expand_actual_items_with_allocator_loop0
  {«Type» : Type} {Value : Type} (corecloneCloneInst : core.clone.Clone
  «Type») (corecloneCloneInst1 : core.clone.Clone Value)
  (iter : core.iter.adapters.enumerate.Enumerate (core.slice.iter.Iter
  (ActualItem «Type» Value)))
  (positional : alloc.vec.Vec (PositionalActual «Type» Value))
  (named : alloc.vec.Vec (NamedActual «Type» Value))
  (evaluation_order : alloc.vec.Vec EvaluationEvent)
  (expansion_error : Option (BindingError «Type»)) :
  Result ((alloc.vec.Vec (PositionalActual «Type» Value)) × (alloc.vec.Vec
    (NamedActual «Type» Value)) × (alloc.vec.Vec EvaluationEvent) × (Option
    (BindingError «Type»)))
  := do
  loop
    (fun (iter1, positional1, named1, evaluation_order1, expansion_error1) =>
      expand_actual_items_with_allocator_loop0.body corecloneCloneInst
      corecloneCloneInst1 iter1 positional1 named1 evaluation_order1
      expansion_error1)
    (iter, positional, named, evaluation_order, expansion_error)

/-- [maledictus_call_binding_extraction::expand_actual_items_with_allocator]:
    Source: 'src/lib.rs', lines 469:0-562:1 -/
def expand_actual_items_with_allocator
  {«Type» : Type} {Value : Type} {Allocator : Type} (corecloneCloneInst :
  core.clone.Clone «Type») (corecloneCloneInst1 : core.clone.Clone Value)
  (BindingAllocatorInst : BindingAllocator Allocator)
  (items : Slice (ActualItem «Type» Value))
  (receiver : Option (Receiver «Type» Value)) (allocator : Allocator) :
  Result ((core.result.Result (ExpandedCall «Type» Value) (BindingError
    «Type»)) × Allocator)
  := do
  let b := core.option.Option.is_some receiver
  let r ← preflight_actual_items items b
  let cf ← core.result.Result.Insts.CoreOpsTry.branch r
  match cf with
  | core.ops.control_flow.ControlFlow.Continue val =>
    let (source_count, expanded_count) := val
    let (r1, allocator1) ←
      allocate_buffer «Type» (PositionalActual «Type» Value)
        BindingAllocatorInst allocator AllocationSite.ExpandedPositionals
        expanded_count
    let cf1 ← core.result.Result.Insts.CoreOpsTry.branch r1
    match cf1 with
    | core.ops.control_flow.ControlFlow.Continue val1 =>
      let (r2, allocator2) ←
        allocate_buffer «Type» (NamedActual «Type» Value)
          BindingAllocatorInst allocator1 AllocationSite.ExpandedNamed
          source_count
      let cf2 ← core.result.Result.Insts.CoreOpsTry.branch r2
      match cf2 with
      | core.ops.control_flow.ControlFlow.Continue val2 =>
        let (r3, allocator3) ←
          allocate_buffer «Type» EvaluationEvent BindingAllocatorInst
            allocator2 AllocationSite.EvaluationOrder source_count
        let cf3 ← core.result.Result.Insts.CoreOpsTry.branch r3
        match cf3 with
        | core.ops.control_flow.ControlFlow.Continue val3 =>
          let (positional, evaluation_order) ←
            match receiver with
            | none => ok (val1, val3)
            | some receiver1 =>
              do
              let val4 ← alloc.vec.Vec.push val3 EvaluationEvent.Receiver
              let val5 ←
                alloc.vec.Vec.push val1
                  ({
                     value := receiver1.value,
                     origin := PositionalOrigin.Receiver,
                     evaluation_position := EvaluationPosition.Receiver
                   } : PositionalActual «Type» Value)
              ok (val5, val4)
          let i ← core.slice.Slice.iter items
          let iter ←
            core.iter.traits.iterator.Iterator.enumerate.trait_default
              (core.iter.traits.iterator.IteratorSliceIter (ActualItem «Type»
              Value)) i
          let (positional1, named, evaluation_order1, expansion_error) ←
            expand_actual_items_with_allocator_loop0 corecloneCloneInst
              corecloneCloneInst1 iter positional val2 evaluation_order none
          match expansion_error with
          | none =>
            ok (core.result.Result.Ok
              {
                positional := positional1,
                named,
                evaluation_order := evaluation_order1
              }, allocator3)
          | some error => ok (core.result.Result.Err error, allocator3)
        | core.ops.control_flow.ControlFlow.Break residual =>
          let r4 ←
            core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
              (ExpandedCall «Type» Value) (core.convert.FromSame
              (BindingError «Type»)) residual
          ok (r4, allocator3)
      | core.ops.control_flow.ControlFlow.Break residual =>
        let r3 ←
          core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
            (ExpandedCall «Type» Value) (core.convert.FromSame (BindingError
            «Type»)) residual
        ok (r3, allocator2)
    | core.ops.control_flow.ControlFlow.Break residual =>
      let r2 ←
        core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
          (ExpandedCall «Type» Value) (core.convert.FromSame (BindingError
          «Type»)) residual
      ok (r2, allocator1)
  | core.ops.control_flow.ControlFlow.Break residual =>
    let r1 ←
      core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
        (ExpandedCall «Type» Value) (core.convert.FromSame (BindingError
        «Type»)) residual
    ok (r1, allocator)

/-- [maledictus_call_binding_extraction::ordinary_parameter_name]: loop body 0:
    Source: 'src/lib.rs', lines 1:0-1095:5 -/
@[rust_loop_body]
def ordinary_parameter_name_loop0.body
  {«Type» : Type} {Value : Type} (signature : CallSignature «Type» Value)
  (candidate : String) (index : Std.Usize) (found : Bool) :
  Result (ControlFlow (Std.Usize × Bool) ((alloc.vec.Vec (FormalParameter
    «Type» Value)) × Bool))
  := do
  let i := alloc.vec.Vec.len signature.positional
  if index < i
  then
    if found
    then ok (done (signature.keyword_only, true))
    else
      let fp ←
        alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
          (FormalParameter «Type» Value)) signature.positional index
      let found1 ←
        alloc.string.String.Insts.CoreCmpPartialEqString.eq fp.name candidate
      let index1 ← index + 1#usize
      ok (cont (index1, found1))
  else ok (done (signature.keyword_only, found))

/-- [maledictus_call_binding_extraction::ordinary_parameter_name]: loop 0:
    Source: 'src/lib.rs', lines 1:0-1095:5 -/
@[rust_loop]
def ordinary_parameter_name_loop0
  {«Type» : Type} {Value : Type} (signature : CallSignature «Type» Value)
  (candidate : String) (index : Std.Usize) (found : Bool) :
  Result ((alloc.vec.Vec (FormalParameter «Type» Value)) × Bool)
  := do
  loop
    (fun (index1, found1) => ordinary_parameter_name_loop0.body signature
      candidate index1 found1)
    (index, found)

/-- [maledictus_call_binding_extraction::ordinary_parameter_name]: loop body 1:
    Source: 'src/lib.rs', lines 1:0-1100:5 -/
@[rust_loop_body]
def ordinary_parameter_name_loop1.body
  {«Type» : Type} {Value : Type}
  (v : alloc.vec.Vec (FormalParameter «Type» Value)) (candidate : String)
  (found : Bool) (index : Std.Usize) :
  Result (ControlFlow (Bool × Std.Usize) Bool)
  := do
  let i := alloc.vec.Vec.len v
  if index < i
  then
    if found
    then ok (done true)
    else
      let fp ←
        alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
          (FormalParameter «Type» Value)) v index
      let found1 ←
        alloc.string.String.Insts.CoreCmpPartialEqString.eq fp.name candidate
      let index1 ← index + 1#usize
      ok (cont (found1, index1))
  else ok (done found)

/-- [maledictus_call_binding_extraction::ordinary_parameter_name]: loop 1:
    Source: 'src/lib.rs', lines 1:0-1100:5 -/
@[rust_loop]
def ordinary_parameter_name_loop1
  {«Type» : Type} {Value : Type}
  (v : alloc.vec.Vec (FormalParameter «Type» Value)) (candidate : String)
  (found : Bool) (index : Std.Usize) :
  Result Bool
  := do
  loop
    (fun (found1, index1) => ordinary_parameter_name_loop1.body v candidate
      found1 index1)
    (found, index)

/-- [maledictus_call_binding_extraction::ordinary_parameter_name]:
    Source: 'src/lib.rs', lines 1086:0-1102:1 -/
def ordinary_parameter_name
  {«Type» : Type} {Value : Type} (signature : CallSignature «Type» Value)
  (candidate : String) :
  Result Bool
  := do
  let (v, found) ←
    ordinary_parameter_name_loop0 signature candidate 0#usize false
  ordinary_parameter_name_loop1 v candidate found 0#usize

/-- [maledictus_call_binding_extraction::named_actual_index]: loop body 0:
    Source: 'src/lib.rs', lines 1:0-662:5 -/
@[rust_loop_body]
def named_actual_index_loop.body
  {«Type» : Type} {Value : Type}
  (actuals : Slice (NamedActual «Type» Value)) («name» : String)
  (actual_count : Std.Usize) (index : Std.Usize) (found : Bool) :
  Result (ControlFlow (Std.Usize × Bool) (Std.Usize × Bool))
  := do
  if index < actual_count
  then
    if found
    then ok (done (index, true))
    else
      let na ← Slice.index_usize actuals index
      let found1 ←
        alloc.string.String.Insts.CoreCmpPartialEqString.eq na.name «name»
      let index1 ← index + 1#usize
      ok (cont (index1, found1))
  else ok (done (index, found))

/-- [maledictus_call_binding_extraction::named_actual_index]: loop 0:
    Source: 'src/lib.rs', lines 1:0-662:5 -/
@[rust_loop]
def named_actual_index_loop
  {«Type» : Type} {Value : Type}
  (actuals : Slice (NamedActual «Type» Value)) («name» : String)
  (actual_count : Std.Usize) (index : Std.Usize) (found : Bool) :
  Result (Std.Usize × Bool)
  := do
  loop
    (fun (index1, found1) => named_actual_index_loop.body actuals «name»
      actual_count index1 found1)
    (index, found)

/-- [maledictus_call_binding_extraction::named_actual_index]:
    Source: 'src/lib.rs', lines 655:0-664:1 -/
def named_actual_index
  {«Type» : Type} {Value : Type}
  (actuals : Slice (NamedActual «Type» Value)) («name» : String) :
  Result Std.Usize
  := do
  let actual_count := Slice.len actuals
  let (index, found) ←
    named_actual_index_loop actuals «name» actual_count 0#usize false
  if found
  then index - 1#usize
  else ok actual_count

/-- [maledictus_call_binding_extraction::find_named_actual]:
    Source: 'src/lib.rs', lines 666:0-676:1 -/
def find_named_actual
  {«Type» : Type} {Value : Type}
  (actuals : Slice (NamedActual «Type» Value)) («name» : String) :
  Result (Option (NamedActual «Type» Value))
  := do
  let index ← named_actual_index actuals «name»
  let i := Slice.len actuals
  if index < i
  then let na ← Slice.index_usize actuals index
       ok (some na)
  else ok none

/-- [maledictus_call_binding_extraction::canonical_environment]: loop body 0:
    Source: 'src/lib.rs', lines 1313:4-1336:5 -/
@[rust_loop_body]
def canonical_environment_loop0.body
  {«Type» : Type} {Value : Type} (corecloneCloneInst : core.clone.Clone
  «Type») (corecloneCloneInst1 : core.clone.Clone Value)
  (positional_only : alloc.vec.Vec (FormalParameter «Type» Value))
  (call : ExpandedCall «Type» Value)
  (cells : alloc.vec.Vec (BindingCell «Type» Value)) (index : Std.Usize)
  (construction_error : Option (BindingError «Type»)) :
  Result (ControlFlow ((ExpandedCall «Type» Value) × (alloc.vec.Vec
    (BindingCell «Type» Value)) × Std.Usize × (Option (BindingError
    «Type»))) ((ExpandedCall «Type» Value) × (alloc.vec.Vec (BindingCell
    «Type» Value)) × (Option (BindingError «Type»))))
  := do
  let i := alloc.vec.Vec.len positional_only
  if index < i
  then
    let parameter ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
        (FormalParameter «Type» Value)) positional_only index
    let b := core.option.Option.is_none construction_error
    let (call1, cells1, construction_error1) ←
      if b
      then
        do
        let s := alloc.vec.Vec.deref call.positional
        let o ←
          core.slice.Slice.get (core.slice.index.SliceIndexUsizeSlice
            (PositionalActual «Type» Value)) s index
        let (parameter1, argument) ←
          match o with
          | none =>
            do
            let (o1, o2) ←
              match parameter.default_value with
              | none => ok (none, none)
              | some default =>
                do
                let tv ←
                  TypedValue.Insts.CoreCloneClone.clone corecloneCloneInst
                    corecloneCloneInst1 default
                ok (parameter.default_value, some (BoundArgument.Defaulted tv))
            ok ({ parameter with default_value := o1 }, o2)
          | some actual =>
            do
            let pa ←
              PositionalActual.Insts.CoreCloneClone.clone corecloneCloneInst
                corecloneCloneInst1 actual
            ok (parameter, some (BoundArgument.SuppliedPositional pa))
        let (v, o1) ←
          match argument with
          | none =>
            do
            let s1 ←
              alloc.string.String.Insts.CoreCloneClone.clone parameter1.name
            ok (cells, some (BindingError.MissingRequiredArgument s1))
          | some argument1 =>
            do
            let fp ←
              FormalParameter.Insts.CoreCloneClone.clone corecloneCloneInst
                corecloneCloneInst1 parameter1
            let cells2 ←
              alloc.vec.Vec.push cells
                ({
                   parameter := fp,
                   kind := ParameterKind.PositionalOnly,
                   argument := argument1
                 } : BindingCell «Type» Value)
            ok (cells2, construction_error)
        ok (call, v, o1)
      else ok (call, cells, construction_error)
    let index1 ← index + 1#usize
    ok (cont (call1, cells1, index1, construction_error1))
  else ok (done (call, cells, construction_error))

/-- [maledictus_call_binding_extraction::canonical_environment]: loop 0:
    Source: 'src/lib.rs', lines 1313:4-1336:5 -/
@[rust_loop]
def canonical_environment_loop0
  {«Type» : Type} {Value : Type} (corecloneCloneInst : core.clone.Clone
  «Type») (corecloneCloneInst1 : core.clone.Clone Value)
  (positional_only : alloc.vec.Vec (FormalParameter «Type» Value))
  (call : ExpandedCall «Type» Value)
  (cells : alloc.vec.Vec (BindingCell «Type» Value)) (index : Std.Usize)
  (construction_error : Option (BindingError «Type»)) :
  Result ((ExpandedCall «Type» Value) × (alloc.vec.Vec (BindingCell «Type»
    Value)) × (Option (BindingError «Type»)))
  := do
  loop
    (fun (call1, cells1, index1, construction_error1) =>
      canonical_environment_loop0.body corecloneCloneInst corecloneCloneInst1
      positional_only call1 cells1 index1 construction_error1)
    (call, cells, index, construction_error)

/-- [maledictus_call_binding_extraction::canonical_environment]: loop body 1:
    Source: 'src/lib.rs', lines 1344:4-1370:5 -/
@[rust_loop_body]
def canonical_environment_loop1.body
  {«Type» : Type} {Value : Type} (corecloneCloneInst : core.clone.Clone
  «Type») (corecloneCloneInst1 : core.clone.Clone Value)
  (positional : alloc.vec.Vec (FormalParameter «Type» Value))
  (positional_offset : Std.Usize) (call : ExpandedCall «Type» Value)
  (cells : alloc.vec.Vec (BindingCell «Type» Value))
  (parameter_index : Std.Usize)
  (construction_error : Option (BindingError «Type»)) :
  Result (ControlFlow ((ExpandedCall «Type» Value) × (alloc.vec.Vec
    (BindingCell «Type» Value)) × Std.Usize × (Option (BindingError
    «Type»))) ((ExpandedCall «Type» Value) × (alloc.vec.Vec (BindingCell
    «Type» Value)) × (Option (BindingError «Type»))))
  := do
  let i := alloc.vec.Vec.len positional
  if parameter_index < i
  then
    let parameter ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
        (FormalParameter «Type» Value)) positional parameter_index
    let index ← positional_offset + parameter_index
    let b := core.option.Option.is_none construction_error
    let (call1, cells1, construction_error1) ←
      if b
      then
        do
        let s := alloc.vec.Vec.deref call.positional
        let o ←
          core.slice.Slice.get (core.slice.index.SliceIndexUsizeSlice
            (PositionalActual «Type» Value)) s index
        let (parameter1, argument) ←
          match o with
          | none =>
            do
            let s1 := alloc.vec.Vec.deref call.named
            let o1 ← find_named_actual s1 parameter.name
            let (o2, o3) ←
              match o1 with
              | none =>
                match parameter.default_value with
                | none => ok (none, none)
                | some default =>
                  do
                  let tv ←
                    TypedValue.Insts.CoreCloneClone.clone corecloneCloneInst
                      corecloneCloneInst1 default
                  ok (parameter.default_value, some (BoundArgument.Defaulted
                    tv))
              | some actual =>
                do
                let na ←
                  NamedActual.Insts.CoreCloneClone.clone corecloneCloneInst
                    corecloneCloneInst1 actual
                ok (parameter.default_value, some (BoundArgument.SuppliedNamed
                  na))
            ok ({ parameter with default_value := o2 }, o3)
          | some actual =>
            do
            let pa ←
              PositionalActual.Insts.CoreCloneClone.clone corecloneCloneInst
                corecloneCloneInst1 actual
            ok (parameter, some (BoundArgument.SuppliedPositional pa))
        let (v, o1) ←
          match argument with
          | none =>
            do
            let s1 ←
              alloc.string.String.Insts.CoreCloneClone.clone parameter1.name
            ok (cells, some (BindingError.MissingRequiredArgument s1))
          | some argument1 =>
            do
            let fp ←
              FormalParameter.Insts.CoreCloneClone.clone corecloneCloneInst
                corecloneCloneInst1 parameter1
            let cells2 ←
              alloc.vec.Vec.push cells
                ({
                   parameter := fp,
                   kind := ParameterKind.Positional,
                   argument := argument1
                 } : BindingCell «Type» Value)
            ok (cells2, construction_error)
        ok (call, v, o1)
      else ok (call, cells, construction_error)
    let parameter_index1 ← parameter_index + 1#usize
    ok (cont (call1, cells1, parameter_index1, construction_error1))
  else ok (done (call, cells, construction_error))

/-- [maledictus_call_binding_extraction::canonical_environment]: loop 1:
    Source: 'src/lib.rs', lines 1344:4-1370:5 -/
@[rust_loop]
def canonical_environment_loop1
  {«Type» : Type} {Value : Type} (corecloneCloneInst : core.clone.Clone
  «Type») (corecloneCloneInst1 : core.clone.Clone Value)
  (positional : alloc.vec.Vec (FormalParameter «Type» Value))
  (call : ExpandedCall «Type» Value)
  (cells : alloc.vec.Vec (BindingCell «Type» Value))
  (positional_offset : Std.Usize) (parameter_index : Std.Usize)
  (construction_error : Option (BindingError «Type»)) :
  Result ((ExpandedCall «Type» Value) × (alloc.vec.Vec (BindingCell «Type»
    Value)) × (Option (BindingError «Type»)))
  := do
  loop
    (fun (call1, cells1, parameter_index1, construction_error1) =>
      canonical_environment_loop1.body corecloneCloneInst corecloneCloneInst1
      positional positional_offset call1 cells1 parameter_index1
      construction_error1)
    (call, cells, parameter_index, construction_error)

/-- [maledictus_call_binding_extraction::canonical_environment]: loop body 2:
    Source: 'src/lib.rs', lines 1377:4-1400:5 -/
@[rust_loop_body]
def canonical_environment_loop2.body
  {«Type» : Type} {Value : Type} (corecloneCloneInst : core.clone.Clone
  «Type») (corecloneCloneInst1 : core.clone.Clone Value)
  (keyword_only : alloc.vec.Vec (FormalParameter «Type» Value))
  (call : ExpandedCall «Type» Value)
  (cells : alloc.vec.Vec (BindingCell «Type» Value))
  (keyword_index : Std.Usize)
  (construction_error : Option (BindingError «Type»)) :
  Result (ControlFlow ((ExpandedCall «Type» Value) × (alloc.vec.Vec
    (BindingCell «Type» Value)) × Std.Usize × (Option (BindingError
    «Type»))) ((ExpandedCall «Type» Value) × (alloc.vec.Vec (BindingCell
    «Type» Value)) × (Option (BindingError «Type»))))
  := do
  let i := alloc.vec.Vec.len keyword_only
  if keyword_index < i
  then
    let parameter ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
        (FormalParameter «Type» Value)) keyword_only keyword_index
    let b := core.option.Option.is_none construction_error
    let (call1, cells1, construction_error1) ←
      if b
      then
        do
        let s := alloc.vec.Vec.deref call.named
        let o ← find_named_actual s parameter.name
        let (o1, argument) ←
          match o with
          | none =>
            match parameter.default_value with
            | none => ok (none, none)
            | some default =>
              do
              let tv ←
                TypedValue.Insts.CoreCloneClone.clone corecloneCloneInst
                  corecloneCloneInst1 default
              ok (parameter.default_value, some (BoundArgument.Defaulted tv))
          | some actual =>
            do
            let na ←
              NamedActual.Insts.CoreCloneClone.clone corecloneCloneInst
                corecloneCloneInst1 actual
            ok (parameter.default_value, some (BoundArgument.SuppliedNamed na))
        let (v, o2) ←
          match argument with
          | none =>
            do
            let s1 ←
              alloc.string.String.Insts.CoreCloneClone.clone parameter.name
            ok (cells, some (BindingError.MissingRequiredArgument s1))
          | some argument1 =>
            do
            let fp ←
              FormalParameter.Insts.CoreCloneClone.clone corecloneCloneInst
                corecloneCloneInst1 { parameter with default_value := o1 }
            let cells2 ←
              alloc.vec.Vec.push cells
                ({
                   parameter := fp,
                   kind := ParameterKind.KeywordOnly,
                   argument := argument1
                 } : BindingCell «Type» Value)
            ok (cells2, construction_error)
        ok (call, v, o2)
      else ok (call, cells, construction_error)
    let keyword_index1 ← keyword_index + 1#usize
    ok (cont (call1, cells1, keyword_index1, construction_error1))
  else ok (done (call, cells, construction_error))

/-- [maledictus_call_binding_extraction::canonical_environment]: loop 2:
    Source: 'src/lib.rs', lines 1377:4-1400:5 -/
@[rust_loop]
def canonical_environment_loop2
  {«Type» : Type} {Value : Type} (corecloneCloneInst : core.clone.Clone
  «Type») (corecloneCloneInst1 : core.clone.Clone Value)
  (keyword_only : alloc.vec.Vec (FormalParameter «Type» Value))
  (call : ExpandedCall «Type» Value)
  (cells : alloc.vec.Vec (BindingCell «Type» Value))
  (keyword_index : Std.Usize)
  (construction_error : Option (BindingError «Type»)) :
  Result ((ExpandedCall «Type» Value) × (alloc.vec.Vec (BindingCell «Type»
    Value)) × (Option (BindingError «Type»)))
  := do
  loop
    (fun (call1, cells1, keyword_index1, construction_error1) =>
      canonical_environment_loop2.body corecloneCloneInst corecloneCloneInst1
      keyword_only call1 cells1 keyword_index1 construction_error1)
    (call, cells, keyword_index, construction_error)

/-- [maledictus_call_binding_extraction::canonical_environment]: loop body 3:
    Source: 'src/lib.rs', lines 1434:8-1440:9 -/
@[rust_loop_body]
def canonical_environment_loop3.body
  {«Type» : Type} {Value : Type} (corecloneCloneInst : core.clone.Clone
  «Type») (corecloneCloneInst1 : core.clone.Clone Value)
  (positional_only : alloc.vec.Vec (FormalParameter «Type» Value))
  (positional : alloc.vec.Vec (FormalParameter «Type» Value))
  (keyword_only : alloc.vec.Vec (FormalParameter «Type» Value))
  (parameter : FormalParameter «Type» Value)
  (v : alloc.vec.Vec (NamedActual «Type» Value))
  (residuals : alloc.vec.Vec (NamedActual «Type» Value)) (index : Std.Usize)
  :
  Result (ControlFlow ((alloc.vec.Vec (NamedActual «Type» Value)) ×
    Std.Usize) (alloc.vec.Vec (NamedActual «Type» Value)))
  := do
  let i := alloc.vec.Vec.len v
  if index < i
  then
    let na ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice (NamedActual
        «Type» Value)) v index
    let actual_name ← alloc.string.String.Insts.CoreCloneClone.clone na.name
    let b ←
      ordinary_parameter_name
        {
          positional_only,
          positional,
          keyword_only,
          var_args := none,
          keyword_args := (some parameter)
        } actual_name
    let residuals1 ←
      if b
      then ok residuals
      else
        do
        let na1 ←
          NamedActual.Insts.CoreCloneClone.clone corecloneCloneInst
            corecloneCloneInst1 na
        alloc.vec.Vec.push residuals na1
    let index1 ← index + 1#usize
    ok (cont (residuals1, index1))
  else ok (done residuals)

/-- [maledictus_call_binding_extraction::canonical_environment]: loop 3:
    Source: 'src/lib.rs', lines 1434:8-1440:9 -/
@[rust_loop]
def canonical_environment_loop3
  {«Type» : Type} {Value : Type} (corecloneCloneInst : core.clone.Clone
  «Type») (corecloneCloneInst1 : core.clone.Clone Value)
  (positional_only : alloc.vec.Vec (FormalParameter «Type» Value))
  (positional : alloc.vec.Vec (FormalParameter «Type» Value))
  (keyword_only : alloc.vec.Vec (FormalParameter «Type» Value))
  (parameter : FormalParameter «Type» Value)
  (v : alloc.vec.Vec (NamedActual «Type» Value))
  (residuals : alloc.vec.Vec (NamedActual «Type» Value)) (index : Std.Usize)
  :
  Result (alloc.vec.Vec (NamedActual «Type» Value))
  := do
  loop
    (fun (residuals1, index1) => canonical_environment_loop3.body
      corecloneCloneInst corecloneCloneInst1 positional_only positional
      keyword_only parameter v residuals1 index1)
    (residuals, index)

/-- [maledictus_call_binding_extraction::canonical_environment]: loop body 4:
    Source: 'src/lib.rs', lines 1417:8-1420:9 -/
@[rust_loop_body]
def canonical_environment_loop4.body
  {«Type» : Type} {Value : Type} (corecloneCloneInst : core.clone.Clone
  «Type») (corecloneCloneInst1 : core.clone.Clone Value)
  (v : alloc.vec.Vec (PositionalActual «Type» Value))
  (residuals : alloc.vec.Vec (PositionalActual «Type» Value))
  (index : Std.Usize) :
  Result (ControlFlow ((alloc.vec.Vec (PositionalActual «Type» Value)) ×
    Std.Usize) (alloc.vec.Vec (PositionalActual «Type» Value)))
  := do
  let i := alloc.vec.Vec.len v
  if index < i
  then
    let pa ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
        (PositionalActual «Type» Value)) v index
    let pa1 ←
      PositionalActual.Insts.CoreCloneClone.clone corecloneCloneInst
        corecloneCloneInst1 pa
    let residuals1 ← alloc.vec.Vec.push residuals pa1
    let index1 ← index + 1#usize
    ok (cont (residuals1, index1))
  else ok (done residuals)

/-- [maledictus_call_binding_extraction::canonical_environment]: loop 4:
    Source: 'src/lib.rs', lines 1417:8-1420:9 -/
@[rust_loop]
def canonical_environment_loop4
  {«Type» : Type} {Value : Type} (corecloneCloneInst : core.clone.Clone
  «Type») (corecloneCloneInst1 : core.clone.Clone Value)
  (v : alloc.vec.Vec (PositionalActual «Type» Value))
  (residuals : alloc.vec.Vec (PositionalActual «Type» Value))
  (index : Std.Usize) :
  Result (alloc.vec.Vec (PositionalActual «Type» Value))
  := do
  loop
    (fun (residuals1, index1) => canonical_environment_loop4.body
      corecloneCloneInst corecloneCloneInst1 v residuals1 index1)
    (residuals, index)

/-- [maledictus_call_binding_extraction::canonical_environment]: loop body 5:
    Source: 'src/lib.rs', lines 1434:8-1440:9 -/
@[rust_loop_body]
def canonical_environment_loop5.body
  {«Type» : Type} {Value : Type} (corecloneCloneInst : core.clone.Clone
  «Type») (corecloneCloneInst1 : core.clone.Clone Value)
  (positional_only : alloc.vec.Vec (FormalParameter «Type» Value))
  (positional : alloc.vec.Vec (FormalParameter «Type» Value))
  (keyword_only : alloc.vec.Vec (FormalParameter «Type» Value))
  (parameter : FormalParameter «Type» Value)
  (parameter1 : FormalParameter «Type» Value)
  (v : alloc.vec.Vec (NamedActual «Type» Value))
  (residuals : alloc.vec.Vec (NamedActual «Type» Value)) (index : Std.Usize)
  :
  Result (ControlFlow ((alloc.vec.Vec (NamedActual «Type» Value)) ×
    Std.Usize) (alloc.vec.Vec (NamedActual «Type» Value)))
  := do
  let i := alloc.vec.Vec.len v
  if index < i
  then
    let na ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice (NamedActual
        «Type» Value)) v index
    let actual_name ← alloc.string.String.Insts.CoreCloneClone.clone na.name
    let b ←
      ordinary_parameter_name
        {
          positional_only,
          positional,
          keyword_only,
          var_args := (some parameter),
          keyword_args := (some parameter1)
        } actual_name
    let residuals1 ←
      if b
      then ok residuals
      else
        do
        let na1 ←
          NamedActual.Insts.CoreCloneClone.clone corecloneCloneInst
            corecloneCloneInst1 na
        alloc.vec.Vec.push residuals na1
    let index1 ← index + 1#usize
    ok (cont (residuals1, index1))
  else ok (done residuals)

/-- [maledictus_call_binding_extraction::canonical_environment]: loop 5:
    Source: 'src/lib.rs', lines 1434:8-1440:9 -/
@[rust_loop]
def canonical_environment_loop5
  {«Type» : Type} {Value : Type} (corecloneCloneInst : core.clone.Clone
  «Type») (corecloneCloneInst1 : core.clone.Clone Value)
  (positional_only : alloc.vec.Vec (FormalParameter «Type» Value))
  (positional : alloc.vec.Vec (FormalParameter «Type» Value))
  (keyword_only : alloc.vec.Vec (FormalParameter «Type» Value))
  (parameter : FormalParameter «Type» Value)
  (parameter1 : FormalParameter «Type» Value)
  (v : alloc.vec.Vec (NamedActual «Type» Value))
  (residuals : alloc.vec.Vec (NamedActual «Type» Value)) (index : Std.Usize)
  :
  Result (alloc.vec.Vec (NamedActual «Type» Value))
  := do
  loop
    (fun (residuals1, index1) => canonical_environment_loop5.body
      corecloneCloneInst corecloneCloneInst1 positional_only positional
      keyword_only parameter parameter1 v residuals1 index1)
    (residuals, index)

/-- [maledictus_call_binding_extraction::canonical_environment]:
    Source: 'src/lib.rs', lines 1293:0-1452:1 -/
def canonical_environment
  {«Type» : Type} {Value : Type} {Allocator : Type} (corecloneCloneInst :
  core.clone.Clone «Type») (corecloneCloneInst1 : core.clone.Clone Value)
  (BindingAllocatorInst : BindingAllocator Allocator)
  (signature : CallSignature «Type» Value)
  (call : ExpandedCall «Type» Value) (counts : SignatureCounts)
  (allocator : Allocator) :
  Result ((core.result.Result (BindingEnvironment «Type» Value) (BindingError
    «Type»)) × Allocator)
  := do
  let (r, allocator1) ←
    allocate_buffer «Type» (BindingCell «Type» Value) BindingAllocatorInst
      allocator AllocationSite.BindingCells counts.parameter_count
  let cf ← core.result.Result.Insts.CoreOpsTry.branch r
  match cf with
  | core.ops.control_flow.ControlFlow.Continue val =>
    let (call1, cells, construction_error) ←
      canonical_environment_loop0 corecloneCloneInst corecloneCloneInst1
        signature.positional_only call val 0#usize none
    match construction_error with
    | none =>
      let positional_offset := alloc.vec.Vec.len signature.positional_only
      let (call2, cells1, construction_error1) ←
        canonical_environment_loop1 corecloneCloneInst corecloneCloneInst1
          signature.positional call1 cells positional_offset 0#usize none
      match construction_error1 with
      | none =>
        let (call3, cells2, construction_error2) ←
          canonical_environment_loop2 corecloneCloneInst corecloneCloneInst1
            signature.keyword_only call2 cells1 0#usize none
        match construction_error2 with
        | none =>
          match signature.var_args with
          | none =>
            match signature.keyword_args with
            | none =>
              ok (core.result.Result.Ok
                { cells := cells2, evaluation_order := call3.evaluation_order },
                allocator1)
            | some parameter =>
              let i := alloc.vec.Vec.len call3.named
              let (r1, allocator2) ←
                allocate_buffer «Type» (NamedActual «Type» Value)
                  BindingAllocatorInst allocator1
                  AllocationSite.ResidualKeywords i
              let cf1 ← core.result.Result.Insts.CoreOpsTry.branch r1
              match cf1 with
              | core.ops.control_flow.ControlFlow.Continue val1 =>
                let residuals ←
                  canonical_environment_loop3 corecloneCloneInst
                    corecloneCloneInst1 signature.positional_only
                    signature.positional signature.keyword_only parameter
                    call3.named val1 0#usize
                let fp ←
                  FormalParameter.Insts.CoreCloneClone.clone corecloneCloneInst
                    corecloneCloneInst1 parameter
                let cells3 ←
                  alloc.vec.Vec.push cells2
                    ({
                       parameter := fp,
                       kind := ParameterKind.KeywordArgs,
                       argument := (BoundArgument.ResidualKeywords residuals)
                     } : BindingCell «Type» Value)
                ok (core.result.Result.Ok
                  { cells := cells3, evaluation_order := call3.evaluation_order
                  }, allocator2)
              | core.ops.control_flow.ControlFlow.Break residual =>
                let r2 ←
                  core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
                    (BindingEnvironment «Type» Value) (core.convert.FromSame
                    (BindingError «Type»)) residual
                ok (r2, allocator2)
          | some parameter =>
            let i := alloc.vec.Vec.len call3.positional
            let residual_count ←
              if i > counts.positional_count
              then
                let i1 := alloc.vec.Vec.len call3.positional
                i1 - counts.positional_count
              else ok 0#usize
            let (r1, allocator2) ←
              allocate_buffer «Type» (PositionalActual «Type» Value)
                BindingAllocatorInst allocator1
                AllocationSite.ResidualPositionals residual_count
            let cf1 ← core.result.Result.Insts.CoreOpsTry.branch r1
            match cf1 with
            | core.ops.control_flow.ControlFlow.Continue val1 =>
              let residuals ←
                canonical_environment_loop4 corecloneCloneInst
                  corecloneCloneInst1 call3.positional val1
                  counts.positional_count
              let fp ←
                FormalParameter.Insts.CoreCloneClone.clone corecloneCloneInst
                  corecloneCloneInst1 parameter
              let cells3 ←
                alloc.vec.Vec.push cells2
                  ({
                     parameter := fp,
                     kind := ParameterKind.VarArgs,
                     argument := (BoundArgument.ResidualPositionals residuals)
                   } : BindingCell «Type» Value)
              match signature.keyword_args with
              | none =>
                ok (core.result.Result.Ok
                  { cells := cells3, evaluation_order := call3.evaluation_order
                  }, allocator2)
              | some parameter1 =>
                let i1 := alloc.vec.Vec.len call3.named
                let (r2, allocator3) ←
                  allocate_buffer «Type» (NamedActual «Type» Value)
                    BindingAllocatorInst allocator2
                    AllocationSite.ResidualKeywords i1
                let cf2 ← core.result.Result.Insts.CoreOpsTry.branch r2
                match cf2 with
                | core.ops.control_flow.ControlFlow.Continue val2 =>
                  let residuals1 ←
                    canonical_environment_loop5 corecloneCloneInst
                      corecloneCloneInst1 signature.positional_only
                      signature.positional signature.keyword_only parameter
                      parameter1 call3.named val2 0#usize
                  let fp1 ←
                    FormalParameter.Insts.CoreCloneClone.clone
                      corecloneCloneInst corecloneCloneInst1 parameter1
                  let cells4 ←
                    alloc.vec.Vec.push cells3
                      ({
                         parameter := fp1,
                         kind := ParameterKind.KeywordArgs,
                         argument :=
                           (BoundArgument.ResidualKeywords residuals1)
                       } : BindingCell «Type» Value)
                  ok (core.result.Result.Ok
                    {
                      cells := cells4,
                      evaluation_order := call3.evaluation_order
                    }, allocator3)
                | core.ops.control_flow.ControlFlow.Break residual =>
                  let r3 ←
                    core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
                      (BindingEnvironment «Type» Value)
                      (core.convert.FromSame (BindingError «Type»)) residual
                  ok (r3, allocator3)
            | core.ops.control_flow.ControlFlow.Break residual =>
              let r2 ←
                core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
                  (BindingEnvironment «Type» Value) (core.convert.FromSame
                  (BindingError «Type»)) residual
              ok (r2, allocator2)
        | some error => ok (core.result.Result.Err error, allocator1)
      | some error => ok (core.result.Result.Err error, allocator1)
    | some error => ok (core.result.Result.Err error, allocator1)
  | core.ops.control_flow.ControlFlow.Break residual =>
    let r1 ←
      core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
        (BindingEnvironment «Type» Value) (core.convert.FromSame
        (BindingError «Type»)) residual
    ok (r1, allocator1)

/-- [maledictus_call_binding_extraction::first_unexpected_keyword]: loop body 0:
    Source: 'src/lib.rs', lines 1:0-1115:5 -/
@[rust_loop_body]
def first_unexpected_keyword_loop.body
  {«Type» : Type} {Value : Type}
  (actuals : Slice (NamedActual «Type» Value))
  (signature : CallSignature «Type» Value) (actual_count : Std.Usize)
  (index : Std.Usize) (found : Bool) :
  Result (ControlFlow (Std.Usize × Bool) (Std.Usize × Bool))
  := do
  if index < actual_count
  then
    if found
    then ok (done (index, true))
    else
      let actual ← Slice.index_usize actuals index
      let b ← ordinary_parameter_name signature actual.name
      let index1 ← index + 1#usize
      ok (cont (index1, ¬ b))
  else ok (done (index, found))

/-- [maledictus_call_binding_extraction::first_unexpected_keyword]: loop 0:
    Source: 'src/lib.rs', lines 1:0-1115:5 -/
@[rust_loop]
def first_unexpected_keyword_loop
  {«Type» : Type} {Value : Type}
  (actuals : Slice (NamedActual «Type» Value))
  (signature : CallSignature «Type» Value) (actual_count : Std.Usize)
  (index : Std.Usize) (found : Bool) :
  Result (Std.Usize × Bool)
  := do
  loop
    (fun (index1, found1) => first_unexpected_keyword_loop.body actuals
      signature actual_count index1 found1)
    (index, found)

/-- [maledictus_call_binding_extraction::first_unexpected_keyword]:
    Source: 'src/lib.rs', lines 1104:0-1117:1 -/
def first_unexpected_keyword
  {«Type» : Type} {Value : Type}
  (actuals : Slice (NamedActual «Type» Value))
  (signature : CallSignature «Type» Value) :
  Result Std.Usize
  := do
  let actual_count := Slice.len actuals
  let (index, found) ←
    first_unexpected_keyword_loop actuals signature actual_count 0#usize false
  if found
  then index - 1#usize
  else ok actual_count

/-- [maledictus_call_binding_extraction::first_extra_named_type_mismatch]: loop body 0:
    Source: 'src/lib.rs', lines 1:0-1062:5 -/
@[rust_loop_body]
def first_extra_named_type_mismatch_loop.body
  {«Type» : Type} {Value : Type} {Compatibility : Type}
  (TypeCompatibilityInst : TypeCompatibility Compatibility «Type»)
  (signature : CallSignature «Type» Value)
  (call : ExpandedCall «Type» Value) (compatibility : Compatibility)
  (parameter : FormalParameter «Type» Value) (index : Std.Usize)
  (mismatch : Bool) :
  Result (ControlFlow ((FormalParameter «Type» Value) × Std.Usize × Bool)
    ((FormalParameter «Type» Value) × (alloc.vec.Vec (NamedActual «Type»
    Value)) × Std.Usize × Bool))
  := do
  let i := alloc.vec.Vec.len call.named
  if index < i
  then
    if mismatch
    then ok (done (parameter, call.named, index, true))
    else
      let na ←
        alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice (NamedActual
          «Type» Value)) call.named index
      let actual_name ←
        alloc.string.String.Insts.CoreCloneClone.clone na.name
      let b ← ordinary_parameter_name signature actual_name
      let (parameter1, mismatch1) ←
        if b
        then ok (parameter, false)
        else
          do
          let mismatch2 ←
            compatibility_mismatch TypeCompatibilityInst compatibility
              parameter.expected_type na.value.type_tag
          ok (parameter, mismatch2)
      let index1 ← index + 1#usize
      ok (cont (parameter1, index1, mismatch1))
  else ok (done (parameter, call.named, index, mismatch))

/-- [maledictus_call_binding_extraction::first_extra_named_type_mismatch]: loop 0:
    Source: 'src/lib.rs', lines 1:0-1062:5 -/
@[rust_loop]
def first_extra_named_type_mismatch_loop
  {«Type» : Type} {Value : Type} {Compatibility : Type}
  (TypeCompatibilityInst : TypeCompatibility Compatibility «Type»)
  (parameter : FormalParameter «Type» Value)
  (signature : CallSignature «Type» Value)
  (call : ExpandedCall «Type» Value) (compatibility : Compatibility)
  (index : Std.Usize) (mismatch : Bool) :
  Result ((FormalParameter «Type» Value) × (alloc.vec.Vec (NamedActual
    «Type» Value)) × Std.Usize × Bool)
  := do
  loop
    (fun (parameter1, index1, mismatch1) =>
      first_extra_named_type_mismatch_loop.body TypeCompatibilityInst signature
      call compatibility parameter1 index1 mismatch1)
    (parameter, index, mismatch)

/-- [maledictus_call_binding_extraction::first_extra_named_type_mismatch]:
    Source: 'src/lib.rs', lines 1040:0-1074:1 -/
def first_extra_named_type_mismatch
  {«Type» : Type} {Value : Type} {Compatibility : Type} (corecloneCloneInst :
  core.clone.Clone «Type») (TypeCompatibilityInst : TypeCompatibility
  Compatibility «Type») (parameter : FormalParameter «Type» Value)
  (signature : CallSignature «Type» Value)
  (call : ExpandedCall «Type» Value) (compatibility : Compatibility) :
  Result (Option (BindingError «Type»))
  := do
  let (parameter1, v, index, mismatch) ←
    first_extra_named_type_mismatch_loop TypeCompatibilityInst parameter
      signature call compatibility 0#usize false
  if mismatch
  then
    let i ← index - 1#usize
    let actual ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice (NamedActual
        «Type» Value)) v i
    let s ← alloc.string.String.Insts.CoreCloneClone.clone parameter1.name
    let t ← corecloneCloneInst.clone parameter1.expected_type
    let t1 ← corecloneCloneInst.clone actual.value.type_tag
    ok (some (BindingError.TypeMismatch s t t1 (some
      actual.evaluation_position)))
  else ok none

/-- [maledictus_call_binding_extraction::first_extra_positional_type_mismatch]: loop body 0:
    Source: 'src/lib.rs', lines 1:0-1026:5 -/
@[rust_loop_body]
def first_extra_positional_type_mismatch_loop.body
  {«Type» : Type} {Value : Type} {Compatibility : Type}
  (TypeCompatibilityInst : TypeCompatibility Compatibility «Type»)
  (parameter : FormalParameter «Type» Value)
  (call : ExpandedCall «Type» Value) (compatibility : Compatibility)
  (index : Std.Usize) (mismatch : Bool) :
  Result (ControlFlow (Std.Usize × Bool) ((FormalParameter «Type» Value) ×
    (alloc.vec.Vec (PositionalActual «Type» Value)) × Std.Usize × Bool))
  := do
  let i := alloc.vec.Vec.len call.positional
  if index < i
  then
    if mismatch
    then ok (done (parameter, call.positional, index, true))
    else
      let pa ←
        alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
          (PositionalActual «Type» Value)) call.positional index
      let mismatch1 ←
        compatibility_mismatch TypeCompatibilityInst compatibility
          parameter.expected_type pa.value.type_tag
      let index1 ← index + 1#usize
      ok (cont (index1, mismatch1))
  else ok (done (parameter, call.positional, index, mismatch))

/-- [maledictus_call_binding_extraction::first_extra_positional_type_mismatch]: loop 0:
    Source: 'src/lib.rs', lines 1:0-1026:5 -/
@[rust_loop]
def first_extra_positional_type_mismatch_loop
  {«Type» : Type} {Value : Type} {Compatibility : Type}
  (TypeCompatibilityInst : TypeCompatibility Compatibility «Type»)
  (parameter : FormalParameter «Type» Value)
  (call : ExpandedCall «Type» Value) (compatibility : Compatibility)
  (index : Std.Usize) (mismatch : Bool) :
  Result ((FormalParameter «Type» Value) × (alloc.vec.Vec (PositionalActual
    «Type» Value)) × Std.Usize × Bool)
  := do
  loop
    (fun (index1, mismatch1) => first_extra_positional_type_mismatch_loop.body
      TypeCompatibilityInst parameter call compatibility index1 mismatch1)
    (index, mismatch)

/-- [maledictus_call_binding_extraction::first_extra_positional_type_mismatch]:
    Source: 'src/lib.rs', lines 1007:0-1038:1 -/
def first_extra_positional_type_mismatch
  {«Type» : Type} {Value : Type} {Compatibility : Type} (corecloneCloneInst :
  core.clone.Clone «Type») (TypeCompatibilityInst : TypeCompatibility
  Compatibility «Type») (parameter : FormalParameter «Type» Value)
  (call : ExpandedCall «Type» Value) (positional_count : Std.Usize)
  (compatibility : Compatibility) :
  Result (Option (BindingError «Type»))
  := do
  let (parameter1, v, index, mismatch) ←
    first_extra_positional_type_mismatch_loop TypeCompatibilityInst parameter
      call compatibility positional_count false
  if mismatch
  then
    let i ← index - 1#usize
    let actual ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
        (PositionalActual «Type» Value)) v i
    let s ← alloc.string.String.Insts.CoreCloneClone.clone parameter1.name
    let t ← corecloneCloneInst.clone parameter1.expected_type
    let t1 ← corecloneCloneInst.clone actual.value.type_tag
    ok (some (BindingError.TypeMismatch s t t1 (some
      actual.evaluation_position)))
  else ok none

/-- [maledictus_call_binding_extraction::first_variadic_type_mismatch]:
    Source: 'src/lib.rs', lines 984:0-1005:1 -/
def first_variadic_type_mismatch
  {«Type» : Type} {Value : Type} {Compatibility : Type} (corecloneCloneInst :
  core.clone.Clone «Type») (TypeCompatibilityInst : TypeCompatibility
  Compatibility «Type») (signature : CallSignature «Type» Value)
  (call : ExpandedCall «Type» Value) (positional_count : Std.Usize)
  (compatibility : Compatibility) :
  Result (Option (BindingError «Type»))
  := do
  let error ←
    match signature.var_args with
    | none => ok none
    | some parameter =>
      first_extra_positional_type_mismatch corecloneCloneInst
        TypeCompatibilityInst parameter call positional_count compatibility
  let b := core.option.Option.is_none error
  if b
  then
    match signature.keyword_args with
    | none => ok error
    | some parameter =>
      first_extra_named_type_mismatch corecloneCloneInst TypeCompatibilityInst
        parameter signature call compatibility
  else ok error

/-- [maledictus_call_binding_extraction::first_keyword_only_type_mismatch]: loop body 0:
    Source: 'src/lib.rs', lines 1:0-966:5 -/
@[rust_loop_body]
def first_keyword_only_type_mismatch_loop.body
  {«Type» : Type} {Value : Type} {Compatibility : Type} (corecloneCloneInst :
  core.clone.Clone «Type») (TypeCompatibilityInst : TypeCompatibility
  Compatibility «Type») (signature : CallSignature «Type» Value)
  (v : alloc.vec.Vec (NamedActual «Type» Value))
  (compatibility : Compatibility) (keyword_index : Std.Usize) (mismatch : Bool)
  (actual_index : Std.Usize) :
  Result (ControlFlow (Std.Usize × Bool × Std.Usize) ((alloc.vec.Vec
    (FormalParameter «Type» Value)) × Std.Usize × Bool × Std.Usize))
  := do
  let i := alloc.vec.Vec.len signature.keyword_only
  if keyword_index < i
  then
    if mismatch
    then ok (done (signature.keyword_only, keyword_index, true, actual_index))
    else
      let fp ←
        alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
          (FormalParameter «Type» Value)) signature.keyword_only
          keyword_index
      let parameter_name ←
        alloc.string.String.Insts.CoreCloneClone.clone fp.name
      let expected_type ← corecloneCloneInst.clone fp.expected_type
      let s := alloc.vec.Vec.deref v
      let candidate_index ← named_actual_index s parameter_name
      let i1 := alloc.vec.Vec.len v
      let (mismatch1, actual_index1) ←
        if candidate_index < i1
        then
          do
          let na ←
            alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
              (NamedActual «Type» Value)) v candidate_index
          let mismatch2 ←
            compatibility_mismatch TypeCompatibilityInst compatibility
              expected_type na.value.type_tag
          ok (mismatch2, candidate_index)
        else ok (false, actual_index)
      let keyword_index1 ← keyword_index + 1#usize
      ok (cont (keyword_index1, mismatch1, actual_index1))
  else
    ok (done (signature.keyword_only, keyword_index, mismatch, actual_index))

/-- [maledictus_call_binding_extraction::first_keyword_only_type_mismatch]: loop 0:
    Source: 'src/lib.rs', lines 1:0-966:5 -/
@[rust_loop]
def first_keyword_only_type_mismatch_loop
  {«Type» : Type} {Value : Type} {Compatibility : Type} (corecloneCloneInst :
  core.clone.Clone «Type») (TypeCompatibilityInst : TypeCompatibility
  Compatibility «Type») (signature : CallSignature «Type» Value)
  (v : alloc.vec.Vec (NamedActual «Type» Value))
  (compatibility : Compatibility) (keyword_index : Std.Usize) (mismatch : Bool)
  (actual_index : Std.Usize) :
  Result ((alloc.vec.Vec (FormalParameter «Type» Value)) × Std.Usize × Bool
    × Std.Usize)
  := do
  loop
    (fun (keyword_index1, mismatch1, actual_index1) =>
      first_keyword_only_type_mismatch_loop.body corecloneCloneInst
      TypeCompatibilityInst signature v compatibility keyword_index1 mismatch1
      actual_index1)
    (keyword_index, mismatch, actual_index)

/-- [maledictus_call_binding_extraction::first_keyword_only_type_mismatch]:
    Source: 'src/lib.rs', lines 941:0-979:1 -/
def first_keyword_only_type_mismatch
  {«Type» : Type} {Value : Type} {Compatibility : Type} (corecloneCloneInst :
  core.clone.Clone «Type») (TypeCompatibilityInst : TypeCompatibility
  Compatibility «Type») (signature : CallSignature «Type» Value)
  (call : ExpandedCall «Type» Value) (compatibility : Compatibility) :
  Result (Option (BindingError «Type»))
  := do
  let actual_index := alloc.vec.Vec.len call.named
  let (v, keyword_index, mismatch, actual_index1) ←
    first_keyword_only_type_mismatch_loop corecloneCloneInst
      TypeCompatibilityInst signature call.named compatibility 0#usize false
      actual_index
  if mismatch
  then
    let i ← keyword_index - 1#usize
    let parameter ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
        (FormalParameter «Type» Value)) v i
    let actual ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice (NamedActual
        «Type» Value)) call.named actual_index1
    let s ← alloc.string.String.Insts.CoreCloneClone.clone parameter.name
    let t ← corecloneCloneInst.clone parameter.expected_type
    let t1 ← corecloneCloneInst.clone actual.value.type_tag
    ok (some (BindingError.TypeMismatch s t t1 (some
      actual.evaluation_position)))
  else ok none

/-- [maledictus_call_binding_extraction::ordinary_actual_mismatch]:
    Source: 'src/lib.rs', lines 906:0-939:1 -/
def ordinary_actual_mismatch
  {«Type» : Type} {Value : Type} {Compatibility : Type}
  (TypeCompatibilityInst : TypeCompatibility Compatibility «Type»)
  (parameter : FormalParameter «Type» Value)
  (call : ExpandedCall «Type» Value) (positional_index : Std.Usize)
  (compatibility : Compatibility) :
  Result (Bool × Std.Usize × Bool)
  := do
  let s := alloc.vec.Vec.deref call.named
  let named_index ← named_actual_index s parameter.name
  let i := alloc.vec.Vec.len call.positional
  if positional_index < i
  then
    let pa ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
        (PositionalActual «Type» Value)) call.positional positional_index
    let b ←
      compatibility_mismatch TypeCompatibilityInst compatibility
        parameter.expected_type pa.value.type_tag
    ok (b, positional_index, false)
  else
    let i1 := alloc.vec.Vec.len call.named
    if named_index < i1
    then
      let na ←
        alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice (NamedActual
          «Type» Value)) call.named named_index
      let b ←
        compatibility_mismatch TypeCompatibilityInst compatibility
          parameter.expected_type na.value.type_tag
      ok (b, named_index, true)
    else ok (false, 0#usize, false)

/-- [maledictus_call_binding_extraction::first_ordinary_type_mismatch]: loop body 0:
    Source: 'src/lib.rs', lines 1:0-881:5 -/
@[rust_loop_body]
def first_ordinary_type_mismatch_loop.body
  {«Type» : Type} {Value : Type} {Compatibility : Type}
  (TypeCompatibilityInst : TypeCompatibility Compatibility «Type»)
  (v : alloc.vec.Vec (FormalParameter «Type» Value))
  (call : ExpandedCall «Type» Value) (compatibility : Compatibility)
  (positional_offset : Std.Usize) (parameter_index : Std.Usize)
  (mismatch : Bool) (actual_index : Std.Usize) (supplied_by_name : Bool) :
  Result (ControlFlow (Std.Usize × Bool × Std.Usize × Bool) (Std.Usize ×
    Bool × Std.Usize × Bool))
  := do
  let i := alloc.vec.Vec.len v
  if parameter_index < i
  then
    if mismatch
    then ok (done (parameter_index, true, actual_index, supplied_by_name))
    else
      let parameter ←
        alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
          (FormalParameter «Type» Value)) v parameter_index
      let index ← positional_offset + parameter_index
      let (mismatch1, actual_index1, supplied_by_name1) ←
        ordinary_actual_mismatch TypeCompatibilityInst parameter call index
          compatibility
      let parameter_index1 ← parameter_index + 1#usize
      ok (cont (parameter_index1, mismatch1, actual_index1, supplied_by_name1))
  else ok (done (parameter_index, mismatch, actual_index, supplied_by_name))

/-- [maledictus_call_binding_extraction::first_ordinary_type_mismatch]: loop 0:
    Source: 'src/lib.rs', lines 1:0-881:5 -/
@[rust_loop]
def first_ordinary_type_mismatch_loop
  {«Type» : Type} {Value : Type} {Compatibility : Type}
  (TypeCompatibilityInst : TypeCompatibility Compatibility «Type»)
  (v : alloc.vec.Vec (FormalParameter «Type» Value))
  (call : ExpandedCall «Type» Value) (compatibility : Compatibility)
  (positional_offset : Std.Usize) (parameter_index : Std.Usize)
  (mismatch : Bool) (actual_index : Std.Usize) (supplied_by_name : Bool) :
  Result (Std.Usize × Bool × Std.Usize × Bool)
  := do
  loop
    (fun (parameter_index1, mismatch1, actual_index1, supplied_by_name1) =>
      first_ordinary_type_mismatch_loop.body TypeCompatibilityInst v call
      compatibility positional_offset parameter_index1 mismatch1 actual_index1
      supplied_by_name1)
    (parameter_index, mismatch, actual_index, supplied_by_name)

/-- [maledictus_call_binding_extraction::first_ordinary_type_mismatch]:
    Source: 'src/lib.rs', lines 859:0-904:1 -/
def first_ordinary_type_mismatch
  {«Type» : Type} {Value : Type} {Compatibility : Type} (corecloneCloneInst :
  core.clone.Clone «Type») (TypeCompatibilityInst : TypeCompatibility
  Compatibility «Type») (signature : CallSignature «Type» Value)
  (call : ExpandedCall «Type» Value) (compatibility : Compatibility) :
  Result (Option (BindingError «Type»))
  := do
  let positional_offset := alloc.vec.Vec.len signature.positional_only
  let (parameter_index, mismatch, actual_index, supplied_by_name) ←
    first_ordinary_type_mismatch_loop TypeCompatibilityInst
      signature.positional call compatibility positional_offset 0#usize false
      0#usize false
  if mismatch
  then
    let i ← parameter_index - 1#usize
    let parameter ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
        (FormalParameter «Type» Value)) signature.positional i
    if supplied_by_name
    then
      let actual ←
        alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice (NamedActual
          «Type» Value)) call.named actual_index
      let s ← alloc.string.String.Insts.CoreCloneClone.clone parameter.name
      let t ← corecloneCloneInst.clone parameter.expected_type
      let t1 ← corecloneCloneInst.clone actual.value.type_tag
      ok (some (BindingError.TypeMismatch s t t1 (some
        actual.evaluation_position)))
    else
      let actual ←
        alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
          (PositionalActual «Type» Value)) call.positional actual_index
      let s ← alloc.string.String.Insts.CoreCloneClone.clone parameter.name
      let t ← corecloneCloneInst.clone parameter.expected_type
      let t1 ← corecloneCloneInst.clone actual.value.type_tag
      ok (some (BindingError.TypeMismatch s t t1 (some
        actual.evaluation_position)))
  else ok none

/-- [maledictus_call_binding_extraction::first_positional_only_type_mismatch]: loop body 0:
    Source: 'src/lib.rs', lines 1:0-844:5 -/
@[rust_loop_body]
def first_positional_only_type_mismatch_loop.body
  {«Type» : Type} {Value : Type} {Compatibility : Type}
  (TypeCompatibilityInst : TypeCompatibility Compatibility «Type»)
  (v : alloc.vec.Vec (FormalParameter «Type» Value))
  (v1 : alloc.vec.Vec (PositionalActual «Type» Value))
  (compatibility : Compatibility) (comparable_count : Std.Usize)
  (index : Std.Usize) (mismatch : Bool) :
  Result (ControlFlow (Std.Usize × Bool) (Std.Usize × Bool))
  := do
  if index < comparable_count
  then
    if mismatch
    then ok (done (index, true))
    else
      let parameter ←
        alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
          (FormalParameter «Type» Value)) v index
      let actual ←
        alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
          (PositionalActual «Type» Value)) v1 index
      let mismatch1 ←
        compatibility_mismatch TypeCompatibilityInst compatibility
          parameter.expected_type actual.value.type_tag
      let index1 ← index + 1#usize
      ok (cont (index1, mismatch1))
  else ok (done (index, mismatch))

/-- [maledictus_call_binding_extraction::first_positional_only_type_mismatch]: loop 0:
    Source: 'src/lib.rs', lines 1:0-844:5 -/
@[rust_loop]
def first_positional_only_type_mismatch_loop
  {«Type» : Type} {Value : Type} {Compatibility : Type}
  (TypeCompatibilityInst : TypeCompatibility Compatibility «Type»)
  (v : alloc.vec.Vec (FormalParameter «Type» Value))
  (v1 : alloc.vec.Vec (PositionalActual «Type» Value))
  (compatibility : Compatibility) (comparable_count : Std.Usize)
  (index : Std.Usize) (mismatch : Bool) :
  Result (Std.Usize × Bool)
  := do
  loop
    (fun (index1, mismatch1) => first_positional_only_type_mismatch_loop.body
      TypeCompatibilityInst v v1 compatibility comparable_count index1
      mismatch1)
    (index, mismatch)

/-- [maledictus_call_binding_extraction::first_positional_only_type_mismatch]:
    Source: 'src/lib.rs', lines 823:0-857:1 -/
def first_positional_only_type_mismatch
  {«Type» : Type} {Value : Type} {Compatibility : Type} (corecloneCloneInst :
  core.clone.Clone «Type») (TypeCompatibilityInst : TypeCompatibility
  Compatibility «Type») (signature : CallSignature «Type» Value)
  (call : ExpandedCall «Type» Value) (compatibility : Compatibility) :
  Result (Option (BindingError «Type»))
  := do
  let i := alloc.vec.Vec.len signature.positional_only
  let i1 := alloc.vec.Vec.len call.positional
  let comparable_count ←
    core.cmp.Ord.min.trait_default core.cmp.OrdUsize i i1
  let (index, mismatch) ←
    first_positional_only_type_mismatch_loop TypeCompatibilityInst
      signature.positional_only call.positional compatibility comparable_count
      0#usize false
  if mismatch
  then
    let i2 ← index - 1#usize
    let parameter ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
        (FormalParameter «Type» Value)) signature.positional_only i2
    let actual ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
        (PositionalActual «Type» Value)) call.positional i2
    let s ← alloc.string.String.Insts.CoreCloneClone.clone parameter.name
    let t ← corecloneCloneInst.clone parameter.expected_type
    let t1 ← corecloneCloneInst.clone actual.value.type_tag
    ok (some (BindingError.TypeMismatch s t t1 (some
      actual.evaluation_position)))
  else ok none

/-- [maledictus_call_binding_extraction::first_missing_keyword_only]: loop body 0:
    Source: 'src/lib.rs', lines 1:0-813:5 -/
@[rust_loop_body]
def first_missing_keyword_only_loop.body
  {«Type» : Type} {Value : Type} (signature : CallSignature «Type» Value)
  (call : ExpandedCall «Type» Value) (keyword_index : Std.Usize)
  (is_missing : Bool) :
  Result (ControlFlow (Std.Usize × Bool) ((alloc.vec.Vec (FormalParameter
    «Type» Value)) × Std.Usize × Bool))
  := do
  let i := alloc.vec.Vec.len signature.keyword_only
  if keyword_index < i
  then
    if is_missing
    then ok (done (signature.keyword_only, keyword_index, true))
    else
      let fp ←
        alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
          (FormalParameter «Type» Value)) signature.keyword_only
          keyword_index
      let parameter_name ←
        alloc.string.String.Insts.CoreCloneClone.clone fp.name
      let has_default := core.option.Option.is_some fp.default_value
      let s := alloc.vec.Vec.deref call.named
      let named_index ← named_actual_index s parameter_name
      let i1 := alloc.vec.Vec.len call.named
      let is_missing1 ←
        if named_index = i1
        then ok (¬ has_default)
        else ok false
      let keyword_index1 ← keyword_index + 1#usize
      ok (cont (keyword_index1, is_missing1))
  else ok (done (signature.keyword_only, keyword_index, is_missing))

/-- [maledictus_call_binding_extraction::first_missing_keyword_only]: loop 0:
    Source: 'src/lib.rs', lines 1:0-813:5 -/
@[rust_loop]
def first_missing_keyword_only_loop
  {«Type» : Type} {Value : Type} (signature : CallSignature «Type» Value)
  (call : ExpandedCall «Type» Value) (keyword_index : Std.Usize)
  (is_missing : Bool) :
  Result ((alloc.vec.Vec (FormalParameter «Type» Value)) × Std.Usize ×
    Bool)
  := do
  loop
    (fun (keyword_index1, is_missing1) => first_missing_keyword_only_loop.body
      signature call keyword_index1 is_missing1)
    (keyword_index, is_missing)

/-- [maledictus_call_binding_extraction::first_missing_keyword_only]:
    Source: 'src/lib.rs', lines 796:0-821:1 -/
def first_missing_keyword_only
  {«Type» : Type} {Value : Type} (corecloneCloneInst : core.clone.Clone
  «Type») (signature : CallSignature «Type» Value)
  (call : ExpandedCall «Type» Value) :
  Result (Option (BindingError «Type»))
  := do
  let (v, keyword_index, is_missing) ←
    first_missing_keyword_only_loop signature call 0#usize false
  if is_missing
  then
    let i ← keyword_index - 1#usize
    let fp ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
        (FormalParameter «Type» Value)) v i
    let s ← alloc.string.String.Insts.CoreCloneClone.clone fp.name
    ok (some (BindingError.MissingRequiredArgument s))
  else ok none

/-- [maledictus_call_binding_extraction::first_missing_ordinary]: loop body 0:
    Source: 'src/lib.rs', lines 1:0-786:5 -/
@[rust_loop_body]
def first_missing_ordinary_loop.body
  {«Type» : Type} {Value : Type}
  (v : alloc.vec.Vec (FormalParameter «Type» Value))
  (call : ExpandedCall «Type» Value) (positional_offset : Std.Usize)
  (parameter_index : Std.Usize) (is_missing : Bool) :
  Result (ControlFlow (Std.Usize × Bool) (Std.Usize × Bool))
  := do
  let i := alloc.vec.Vec.len v
  if parameter_index < i
  then
    if is_missing
    then ok (done (parameter_index, true))
    else
      let index ← positional_offset + parameter_index
      let fp ←
        alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
          (FormalParameter «Type» Value)) v parameter_index
      let parameter_name ←
        alloc.string.String.Insts.CoreCloneClone.clone fp.name
      let has_default := core.option.Option.is_some fp.default_value
      let s := alloc.vec.Vec.deref call.named
      let named_index ← named_actual_index s parameter_name
      let i1 := alloc.vec.Vec.len call.positional
      let is_missing1 ←
        if index >= i1
        then
          let i2 := alloc.vec.Vec.len call.named
          if named_index = i2
          then ok (¬ has_default)
          else ok false
        else ok false
      let parameter_index1 ← parameter_index + 1#usize
      ok (cont (parameter_index1, is_missing1))
  else ok (done (parameter_index, is_missing))

/-- [maledictus_call_binding_extraction::first_missing_ordinary]: loop 0:
    Source: 'src/lib.rs', lines 1:0-786:5 -/
@[rust_loop]
def first_missing_ordinary_loop
  {«Type» : Type} {Value : Type}
  (v : alloc.vec.Vec (FormalParameter «Type» Value))
  (call : ExpandedCall «Type» Value) (positional_offset : Std.Usize)
  (parameter_index : Std.Usize) (is_missing : Bool) :
  Result (Std.Usize × Bool)
  := do
  loop
    (fun (parameter_index1, is_missing1) => first_missing_ordinary_loop.body v
      call positional_offset parameter_index1 is_missing1)
    (parameter_index, is_missing)

/-- [maledictus_call_binding_extraction::first_missing_ordinary]:
    Source: 'src/lib.rs', lines 766:0-794:1 -/
def first_missing_ordinary
  {«Type» : Type} {Value : Type} (corecloneCloneInst : core.clone.Clone
  «Type») (signature : CallSignature «Type» Value)
  (call : ExpandedCall «Type» Value) :
  Result (Option (BindingError «Type»))
  := do
  let positional_offset := alloc.vec.Vec.len signature.positional_only
  let (parameter_index, is_missing) ←
    first_missing_ordinary_loop signature.positional call positional_offset
      0#usize false
  if is_missing
  then
    let i ← parameter_index - 1#usize
    let fp ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
        (FormalParameter «Type» Value)) signature.positional i
    let s ← alloc.string.String.Insts.CoreCloneClone.clone fp.name
    ok (some (BindingError.MissingRequiredArgument s))
  else ok none

/-- [maledictus_call_binding_extraction::first_missing_positional_only]: loop body 0:
    Source: 'src/lib.rs', lines 1:0-756:5 -/
@[rust_loop_body]
def first_missing_positional_only_loop.body
  {«Type» : Type} {Value : Type}
  (v : alloc.vec.Vec (FormalParameter «Type» Value))
  (call : ExpandedCall «Type» Value) (parameter_count : Std.Usize)
  (index : Std.Usize) (is_missing : Bool) :
  Result (ControlFlow (Std.Usize × Bool) (Std.Usize × Bool))
  := do
  if index < parameter_count
  then
    if is_missing
    then ok (done (index, true))
    else
      let parameter ←
        alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
          (FormalParameter «Type» Value)) v index
      let i := alloc.vec.Vec.len call.positional
      let is_missing1 ←
        if index >= i
        then ok (core.option.Option.is_none parameter.default_value)
        else ok false
      let index1 ← index + 1#usize
      ok (cont (index1, is_missing1))
  else ok (done (index, is_missing))

/-- [maledictus_call_binding_extraction::first_missing_positional_only]: loop 0:
    Source: 'src/lib.rs', lines 1:0-756:5 -/
@[rust_loop]
def first_missing_positional_only_loop
  {«Type» : Type} {Value : Type}
  (v : alloc.vec.Vec (FormalParameter «Type» Value))
  (call : ExpandedCall «Type» Value) (parameter_count : Std.Usize)
  (index : Std.Usize) (is_missing : Bool) :
  Result (Std.Usize × Bool)
  := do
  loop
    (fun (index1, is_missing1) => first_missing_positional_only_loop.body v
      call parameter_count index1 is_missing1)
    (index, is_missing)

/-- [maledictus_call_binding_extraction::first_missing_positional_only]:
    Source: 'src/lib.rs', lines 742:0-764:1 -/
def first_missing_positional_only
  {«Type» : Type} {Value : Type} (corecloneCloneInst : core.clone.Clone
  «Type») (signature : CallSignature «Type» Value)
  (call : ExpandedCall «Type» Value) :
  Result (Option (BindingError «Type»))
  := do
  let parameter_count := alloc.vec.Vec.len signature.positional_only
  let (index, is_missing) ←
    first_missing_positional_only_loop signature.positional_only call
      parameter_count 0#usize false
  if is_missing
  then
    let i ← index - 1#usize
    let fp ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
        (FormalParameter «Type» Value)) signature.positional_only i
    let s ← alloc.string.String.Insts.CoreCloneClone.clone fp.name
    ok (some (BindingError.MissingRequiredArgument s))
  else ok none

/-- [maledictus_call_binding_extraction::first_missing_required_argument]:
    Source: 'src/lib.rs', lines 725:0-740:1 -/
def first_missing_required_argument
  {«Type» : Type} {Value : Type} (corecloneCloneInst : core.clone.Clone
  «Type») (signature : CallSignature «Type» Value)
  (call : ExpandedCall «Type» Value) :
  Result (Option (BindingError «Type»))
  := do
  let error ← first_missing_positional_only corecloneCloneInst signature call
  let b := core.option.Option.is_none error
  let error1 ←
    if b
    then first_missing_ordinary corecloneCloneInst signature call
    else ok error
  let b1 := core.option.Option.is_none error1
  if b1
  then first_missing_keyword_only corecloneCloneInst signature call
  else ok error1

/-- [maledictus_call_binding_extraction::first_duplicate_binding]: loop body 0:
    Source: 'src/lib.rs', lines 1:0-715:5 -/
@[rust_loop_body]
def first_duplicate_binding_loop.body
  {«Type» : Type} {Value : Type}
  (v : alloc.vec.Vec (FormalParameter «Type» Value))
  (call : ExpandedCall «Type» Value) (ordinary_position : Std.Usize)
  (parameter_index : Std.Usize) (duplicate : Bool) :
  Result (ControlFlow (Std.Usize × Std.Usize × Bool) (Std.Usize × Bool))
  := do
  let i := alloc.vec.Vec.len v
  if parameter_index < i
  then
    if duplicate
    then ok (done (parameter_index, true))
    else
      let parameter ←
        alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
          (FormalParameter «Type» Value)) v parameter_index
      let i1 := alloc.vec.Vec.len call.positional
      let duplicate1 ←
        if ordinary_position < i1
        then
          do
          let s := alloc.vec.Vec.deref call.named
          let i2 ← named_actual_index s parameter.name
          let i3 := alloc.vec.Vec.len call.named
          ok (i2 < i3)
        else ok false
      let ordinary_position1 ← ordinary_position + 1#usize
      let parameter_index1 ← parameter_index + 1#usize
      ok (cont (ordinary_position1, parameter_index1, duplicate1))
  else ok (done (parameter_index, duplicate))

/-- [maledictus_call_binding_extraction::first_duplicate_binding]: loop 0:
    Source: 'src/lib.rs', lines 1:0-715:5 -/
@[rust_loop]
def first_duplicate_binding_loop
  {«Type» : Type} {Value : Type}
  (v : alloc.vec.Vec (FormalParameter «Type» Value))
  (call : ExpandedCall «Type» Value) (ordinary_position : Std.Usize)
  (parameter_index : Std.Usize) (duplicate : Bool) :
  Result (Std.Usize × Bool)
  := do
  loop
    (fun (ordinary_position1, parameter_index1, duplicate1) =>
      first_duplicate_binding_loop.body v call ordinary_position1
      parameter_index1 duplicate1)
    (ordinary_position, parameter_index, duplicate)

/-- [maledictus_call_binding_extraction::first_duplicate_binding]:
    Source: 'src/lib.rs', lines 699:0-723:1 -/
def first_duplicate_binding
  {«Type» : Type} {Value : Type} (corecloneCloneInst : core.clone.Clone
  «Type») (signature : CallSignature «Type» Value)
  (call : ExpandedCall «Type» Value) :
  Result (Option (BindingError «Type»))
  := do
  let ordinary_position := alloc.vec.Vec.len signature.positional_only
  let (parameter_index, duplicate) ←
    first_duplicate_binding_loop signature.positional call ordinary_position
      0#usize false
  if duplicate
  then
    let i ← parameter_index - 1#usize
    let fp ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
        (FormalParameter «Type» Value)) signature.positional i
    let s ← alloc.string.String.Insts.CoreCloneClone.clone fp.name
    ok (some (BindingError.DuplicateBinding s))
  else ok none

/-- [maledictus_call_binding_extraction::first_duplicate_named_actual]: loop body 1:
    Source: 'src/lib.rs', lines 1:0-690:9 -/
@[rust_loop_body]
def first_duplicate_named_actual_loop0_loop0.body
  {«Type» : Type} {Value : Type}
  (actuals : Slice (NamedActual «Type» Value)) (outer_index : Std.Usize)
  (candidate : String) (inner_index : Std.Usize) (seen : Bool) :
  Result (ControlFlow (Std.Usize × Bool) Bool)
  := do
  if inner_index < outer_index
  then
    if seen
    then ok (done true)
    else
      let na ← Slice.index_usize actuals inner_index
      let seen1 ←
        alloc.string.String.Insts.CoreCmpPartialEqString.eq na.name candidate
      let inner_index1 ← inner_index + 1#usize
      ok (cont (inner_index1, seen1))
  else ok (done seen)

/-- [maledictus_call_binding_extraction::first_duplicate_named_actual]: loop 1:
    Source: 'src/lib.rs', lines 1:0-690:9 -/
@[rust_loop]
def first_duplicate_named_actual_loop0_loop0
  {«Type» : Type} {Value : Type}
  (actuals : Slice (NamedActual «Type» Value)) (outer_index : Std.Usize)
  (candidate : String) (inner_index : Std.Usize) (seen : Bool) :
  Result Bool
  := do
  loop
    (fun (inner_index1, seen1) => first_duplicate_named_actual_loop0_loop0.body
      actuals outer_index candidate inner_index1 seen1)
    (inner_index, seen)

/-- [maledictus_call_binding_extraction::first_duplicate_named_actual]: loop body 0:
    Source: 'src/lib.rs', lines 1:0-695:5 -/
@[rust_loop_body]
def first_duplicate_named_actual_loop0.body
  {«Type» : Type} {Value : Type}
  (actuals : Slice (NamedActual «Type» Value)) (outer_index : Std.Usize)
  (duplicate : Option String) :
  Result (ControlFlow (Std.Usize × (Option String)) (Option String))
  := do
  let i := Slice.len actuals
  if outer_index < i
  then
    let b := core.option.Option.is_none duplicate
    if b
    then
      let na ← Slice.index_usize actuals outer_index
      let candidate ← alloc.string.String.Insts.CoreCloneClone.clone na.name
      let seen ←
        first_duplicate_named_actual_loop0_loop0 actuals outer_index candidate
          0#usize false
      let duplicate1 ← if seen
                         then ok (some candidate)
                         else ok duplicate
      let outer_index1 ← outer_index + 1#usize
      ok (cont (outer_index1, duplicate1))
    else ok (done duplicate)
  else ok (done duplicate)

/-- [maledictus_call_binding_extraction::first_duplicate_named_actual]: loop 0:
    Source: 'src/lib.rs', lines 1:0-695:5 -/
@[rust_loop]
def first_duplicate_named_actual_loop0
  {«Type» : Type} {Value : Type}
  (actuals : Slice (NamedActual «Type» Value)) (outer_index : Std.Usize)
  (duplicate : Option String) :
  Result (Option String)
  := do
  loop
    (fun (outer_index1, duplicate1) => first_duplicate_named_actual_loop0.body
      actuals outer_index1 duplicate1)
    (outer_index, duplicate)

/-- [maledictus_call_binding_extraction::first_duplicate_named_actual]:
    Source: 'src/lib.rs', lines 678:0-697:1 -/
@[reducible]
def first_duplicate_named_actual
  {«Type» : Type} {Value : Type}
  (actuals : Slice (NamedActual «Type» Value)) :
  Result (Option String)
  := do
  first_duplicate_named_actual_loop0 actuals 0#usize none

/-- [maledictus_call_binding_extraction::validate_call]:
    Source: 'src/lib.rs', lines 1238:0-1289:1 -/
def validate_call
  {«Type» : Type} {Value : Type} {Compatibility : Type} (corecloneCloneInst :
  core.clone.Clone «Type») (TypeCompatibilityInst : TypeCompatibility
  Compatibility «Type») (signature : CallSignature «Type» Value)
  (call : ExpandedCall «Type» Value) (positional_count : Std.Usize)
  (compatibility : Compatibility) :
  Result (core.result.Result Unit (BindingError «Type»))
  := do
  let s := alloc.vec.Vec.deref call.named
  let o ← first_duplicate_named_actual s
  match o with
  | none =>
    let o1 ← first_duplicate_binding corecloneCloneInst signature call
    match o1 with
    | none =>
      let i := alloc.vec.Vec.len call.positional
      if i > positional_count
      then
        let b := core.option.Option.is_none signature.var_args
        if b
        then
          let i1 := alloc.vec.Vec.len call.positional
          ok (core.result.Result.Err (BindingError.TooManyPositionals
            positional_count i1))
        else
          let s1 := alloc.vec.Vec.deref call.named
          let unexpected_keyword ← first_unexpected_keyword s1 signature
          let b1 := core.option.Option.is_none signature.keyword_args
          if b1
          then
            let i1 := alloc.vec.Vec.len call.named
            if unexpected_keyword < i1
            then
              let na ←
                alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
                  (NamedActual «Type» Value)) call.named unexpected_keyword
              let s2 ← alloc.string.String.Insts.CoreCloneClone.clone na.name
              ok (core.result.Result.Err (BindingError.UnexpectedKeyword s2))
            else
              let o2 ←
                first_missing_required_argument corecloneCloneInst signature
                  call
              match o2 with
              | none =>
                let o3 ←
                  first_positional_only_type_mismatch corecloneCloneInst
                    TypeCompatibilityInst signature call compatibility
                match o3 with
                | none =>
                  let o4 ←
                    first_ordinary_type_mismatch corecloneCloneInst
                      TypeCompatibilityInst signature call compatibility
                  match o4 with
                  | none =>
                    let o5 ←
                      first_keyword_only_type_mismatch corecloneCloneInst
                        TypeCompatibilityInst signature call compatibility
                    match o5 with
                    | none =>
                      let o6 ←
                        first_variadic_type_mismatch corecloneCloneInst
                          TypeCompatibilityInst signature call positional_count
                          compatibility
                      match o6 with
                      | none => ok (core.result.Result.Ok ())
                      | some error => ok (core.result.Result.Err error)
                    | some error => ok (core.result.Result.Err error)
                  | some error => ok (core.result.Result.Err error)
                | some error => ok (core.result.Result.Err error)
              | some error => ok (core.result.Result.Err error)
          else
            let o2 ←
              first_missing_required_argument corecloneCloneInst signature call
            match o2 with
            | none =>
              let o3 ←
                first_positional_only_type_mismatch corecloneCloneInst
                  TypeCompatibilityInst signature call compatibility
              match o3 with
              | none =>
                let o4 ←
                  first_ordinary_type_mismatch corecloneCloneInst
                    TypeCompatibilityInst signature call compatibility
                match o4 with
                | none =>
                  let o5 ←
                    first_keyword_only_type_mismatch corecloneCloneInst
                      TypeCompatibilityInst signature call compatibility
                  match o5 with
                  | none =>
                    let o6 ←
                      first_variadic_type_mismatch corecloneCloneInst
                        TypeCompatibilityInst signature call positional_count
                        compatibility
                    match o6 with
                    | none => ok (core.result.Result.Ok ())
                    | some error => ok (core.result.Result.Err error)
                  | some error => ok (core.result.Result.Err error)
                | some error => ok (core.result.Result.Err error)
              | some error => ok (core.result.Result.Err error)
            | some error => ok (core.result.Result.Err error)
      else
        let s1 := alloc.vec.Vec.deref call.named
        let unexpected_keyword ← first_unexpected_keyword s1 signature
        let b := core.option.Option.is_none signature.keyword_args
        if b
        then
          let i1 := alloc.vec.Vec.len call.named
          if unexpected_keyword < i1
          then
            let na ←
              alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
                (NamedActual «Type» Value)) call.named unexpected_keyword
            let s2 ← alloc.string.String.Insts.CoreCloneClone.clone na.name
            ok (core.result.Result.Err (BindingError.UnexpectedKeyword s2))
          else
            let o2 ←
              first_missing_required_argument corecloneCloneInst signature call
            match o2 with
            | none =>
              let o3 ←
                first_positional_only_type_mismatch corecloneCloneInst
                  TypeCompatibilityInst signature call compatibility
              match o3 with
              | none =>
                let o4 ←
                  first_ordinary_type_mismatch corecloneCloneInst
                    TypeCompatibilityInst signature call compatibility
                match o4 with
                | none =>
                  let o5 ←
                    first_keyword_only_type_mismatch corecloneCloneInst
                      TypeCompatibilityInst signature call compatibility
                  match o5 with
                  | none =>
                    let o6 ←
                      first_variadic_type_mismatch corecloneCloneInst
                        TypeCompatibilityInst signature call positional_count
                        compatibility
                    match o6 with
                    | none => ok (core.result.Result.Ok ())
                    | some error => ok (core.result.Result.Err error)
                  | some error => ok (core.result.Result.Err error)
                | some error => ok (core.result.Result.Err error)
              | some error => ok (core.result.Result.Err error)
            | some error => ok (core.result.Result.Err error)
        else
          let o2 ←
            first_missing_required_argument corecloneCloneInst signature call
          match o2 with
          | none =>
            let o3 ←
              first_positional_only_type_mismatch corecloneCloneInst
                TypeCompatibilityInst signature call compatibility
            match o3 with
            | none =>
              let o4 ←
                first_ordinary_type_mismatch corecloneCloneInst
                  TypeCompatibilityInst signature call compatibility
              match o4 with
              | none =>
                let o5 ←
                  first_keyword_only_type_mismatch corecloneCloneInst
                    TypeCompatibilityInst signature call compatibility
                match o5 with
                | none =>
                  let o6 ←
                    first_variadic_type_mismatch corecloneCloneInst
                      TypeCompatibilityInst signature call positional_count
                      compatibility
                  match o6 with
                  | none => ok (core.result.Result.Ok ())
                  | some error => ok (core.result.Result.Err error)
                | some error => ok (core.result.Result.Err error)
              | some error => ok (core.result.Result.Err error)
            | some error => ok (core.result.Result.Err error)
          | some error => ok (core.result.Result.Err error)
    | some error => ok (core.result.Result.Err error)
  | some «name» =>
    ok (core.result.Result.Err (BindingError.DuplicateNamedArgument «name»))

/-- [maledictus_call_binding_extraction::bind_expanded_call_with]:
    Source: 'src/lib.rs', lines 440:0-455:1 -/
def bind_expanded_call_with
  {«Type» : Type} {Value : Type} {Compatibility : Type} {Allocator : Type}
  (corecloneCloneInst : core.clone.Clone «Type») (corecloneCloneInst1 :
  core.clone.Clone Value) (TypeCompatibilityInst : TypeCompatibility
  Compatibility «Type») (BindingAllocatorInst : BindingAllocator Allocator)
  (signature : CallSignature «Type» Value)
  (expanded : ExpandedCall «Type» Value) (counts : SignatureCounts)
  (compatibility : Compatibility) (allocator : Allocator) :
  Result ((core.result.Result (BindingEnvironment «Type» Value) (BindingError
    «Type»)) × Allocator)
  := do
  let r ←
    validate_call corecloneCloneInst TypeCompatibilityInst signature expanded
      counts.positional_count compatibility
  let cf ← core.result.Result.Insts.CoreOpsTry.branch r
  match cf with
  | core.ops.control_flow.ControlFlow.Continue _ =>
    canonical_environment corecloneCloneInst corecloneCloneInst1
      BindingAllocatorInst signature expanded counts allocator
  | core.ops.control_flow.ControlFlow.Break residual =>
    let r1 ←
      core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
        (BindingEnvironment «Type» Value) (core.convert.FromSame
        (BindingError «Type»)) residual
    ok (r1, allocator)

/-- [maledictus_call_binding_extraction::bind_call_with_relation]:
    Source: 'src/lib.rs', lines 416:0-432:1 -/
def bind_call_with_relation
  {«Type» : Type} {Value : Type} {Compatibility : Type} {Allocator : Type}
  (corecloneCloneInst : core.clone.Clone «Type») (corecloneCloneInst1 :
  core.clone.Clone Value) (TypeCompatibilityInst : TypeCompatibility
  Compatibility «Type») (BindingAllocatorInst : BindingAllocator Allocator)
  (signature : CallSignature «Type» Value)
  (items : Slice (ActualItem «Type» Value))
  (receiver : Option (Receiver «Type» Value)) (compatibility : Compatibility)
  (allocator : Allocator) :
  Result ((core.result.Result (BindingEnvironment «Type» Value) (BindingError
    «Type»)) × Allocator)
  := do
  let (r, allocator1) ←
    validate_signature corecloneCloneInst TypeCompatibilityInst
      BindingAllocatorInst signature compatibility allocator
  let cf ← core.result.Result.Insts.CoreOpsTry.branch r
  match cf with
  | core.ops.control_flow.ControlFlow.Continue val =>
    let (r1, allocator2) ←
      expand_actual_items_with_allocator corecloneCloneInst corecloneCloneInst1
        BindingAllocatorInst items receiver allocator1
    let cf1 ← core.result.Result.Insts.CoreOpsTry.branch r1
    match cf1 with
    | core.ops.control_flow.ControlFlow.Continue val1 =>
      bind_expanded_call_with corecloneCloneInst corecloneCloneInst1
        TypeCompatibilityInst BindingAllocatorInst signature val1 val
        compatibility allocator2
    | core.ops.control_flow.ControlFlow.Break residual =>
      let r2 ←
        core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
          (BindingEnvironment «Type» Value) (core.convert.FromSame
          (BindingError «Type»)) residual
      ok (r2, allocator2)
  | core.ops.control_flow.ControlFlow.Break residual =>
    let r1 ←
      core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
        (BindingEnvironment «Type» Value) (core.convert.FromSame
        (BindingError «Type»)) residual
    ok (r1, allocator1)

/-- [maledictus_call_binding_extraction::bind_call_with_allocator]:
    Source: 'src/lib.rs', lines 372:0-390:1 -/
def bind_call_with_allocator
  {«Type» : Type} {Value : Type} {Allocator : Type} (corecloneCloneInst :
  core.clone.Clone «Type») (corecmpEqInst : core.cmp.Eq «Type»)
  (corecloneCloneInst1 : core.clone.Clone Value) (BindingAllocatorInst :
  BindingAllocator Allocator) (signature : CallSignature «Type» Value)
  (items : Slice (ActualItem «Type» Value))
  (receiver : Option (Receiver «Type» Value)) (allocator : Allocator) :
  Result ((core.result.Result (BindingEnvironment «Type» Value) (BindingError
    «Type»)) × Allocator)
  := do
  bind_call_with_relation corecloneCloneInst corecloneCloneInst1
    (ExactTypeCompatibility.Insts.Maledictus_call_binding_extractionTypeCompatibility
    corecmpEqInst) BindingAllocatorInst signature items receiver () allocator

/-- [maledictus_call_binding_extraction::bind_call_with]:
    Source: 'src/lib.rs', lines 396:0-414:1
    Visibility: public -/
def bind_call_with
  {«Type» : Type} {Value : Type} {Compatible : Type} (corecloneCloneInst :
  core.clone.Clone «Type») (corecloneCloneInst1 : core.clone.Clone Value)
  (coreopsfunctionFnCompatiblePairShared0TypeShared1TypeBoolInst :
  core.ops.function.Fn Compatible («Type» × «Type») Bool)
  (signature : CallSignature «Type» Value)
  (items : Slice (ActualItem «Type» Value))
  (receiver : Option (Receiver «Type» Value)) (compatible : Compatible) :
  Result (core.result.Result (BindingEnvironment «Type» Value) (BindingError
    «Type»))
  := do
  let (r, _) ←
    bind_call_with_relation corecloneCloneInst corecloneCloneInst1
      (CallbackTypeCompatibility.Insts.Maledictus_call_binding_extractionTypeCompatibility
      coreopsfunctionFnCompatiblePairShared0TypeShared1TypeBoolInst)
      SystemBindingAllocator.Insts.Maledictus_call_binding_extractionBindingAllocator
      signature items receiver compatible ()
  ok r

end BindCallFull
